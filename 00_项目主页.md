# 美股泡沫研究

这是这个 Obsidian 文档库的入口页。建议在其他设备上直接把本项目文件夹作为 Obsidian Vault 打开。

## 常用入口

- [[README|项目说明]]
- [[跨设备同步说明|跨设备同步说明]]
- [[bubble_top_signal_tool_design|顶部泡沫预警工具设计]]
- [[ponzi_funnel_validation|泡沫漏斗验证]]
- [[ai_bubble_second_meltup_and_micron_canary|AI 泡沫二波融涨与美光金丝雀]]
- [[docs/nasdaq_bottom_framework_backtest|NASDAQ 底部框架回测]]

## 当前状态

更新：2026-07-07 18:47 北京时间

- Dashboard 已联网刷新，市场数据截至 2026-07-06。
- 当前泡沫风险：`Orange / 61.6`。价格确认 `66.2`，FINRA 慢变量 `100.0`，日频杠杆代理 `19.2`，AI 脆弱性叠加层 `46.1`。
- 三组顶部信号已接入免费自动源：AI CapEx 使用 SEC Companyfacts，流动性使用 FRED / New York Fed，期权拥挤使用 Cboe 免费成交量代理。当前分项：AI CapEx `48.6`，流动性抽水 `41.4`，期权机械买盘 `48.4`。
- Dashboard 已新增“数据新鲜度与置信度”面板：当前自动覆盖率 `69.4%`，新鲜度 `97.5`，置信度 `73.8`，无未知 freshness。
- 美光金丝雀工具已接入 dashboard：当前 `Hot / 72.3`。按 2026-07-06 MU 收盘价 `984.75` 和 TTM GAAP EPS 约 `44.24` 估算，追踪 P/E 约 `22.3x`。
- 当前底部就绪度：`9.2 / 20`，状态为 `No setup`。QQQ 距 52 周高点回撤约 `-3.0%`，未达到底部模型的有效设置区。
- 底部回测已更新到 19 个 QQQ 回撤事件，当前建议阈值仍为 `13 / 20`。
- 本地 Git 元数据存在缺失对象和 `.DS_Store` 误入 `.git/refs` 的问题；研究文件和脚本可继续使用，但提交/差异查看前需要先修复仓库元数据。

## 下一步

1. 修复 Git 仓库状态，避免 Obsidian / Finder 生成文件污染 `.git`。
2. 继续替换剩余手动指标，优先是估值、广度、IBKR 月度代理和精确 0DTE stress share。
3. 验证 Cboe 免费代理与真实 0DTE / dealer gamma 的相关性，避免把免费成交量代理解释过度。
4. 修复 Git 仓库元数据后，将 workflow 文件同步为 `requirements.txt` 安装版本。

## 本地仪表盘

生成最新数据：

```powershell
python scripts/update_data.py
```

启动本地仪表盘：

```powershell
python -m http.server 8000 -d docs
```

然后打开：

```text
http://localhost:8000
```

## 关键文件

- `config/indicators.json`: 手动指标输入。
- `scripts/update_data.py`: 更新 dashboard 数据。
- `scripts/backtest_bottom_framework.py`: 重新跑底部框架回测。
- `docs/`: 静态网页仪表盘。
- `data/`: 原始数据缓存和回测输出。
