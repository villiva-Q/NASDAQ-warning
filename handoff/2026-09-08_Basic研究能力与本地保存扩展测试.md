# Basic 研究能力与本地保存扩展测试

用户最新指令（2026-09-08）：邮件已发，在 Basic 上尽量充分测试，包括长期本地保存，检查 Basic 的权限是否够研究使用。

本工单受《内部项目最小防护与工程纪律 v1.0》约束。不得新增该文件第4节所列防护；除第5节停止条件外，不得因范围、沙箱、审计轮次、测试环境或文件清单请求人工批准。

## 最新授权与范围

用户已明确批准测试真实响应落盘、本地保存与研究可用性。此前本线程自行收窄的“只做内存测试、不保存原始行情或数值、不做本地研究重放”限制被本次指令取代。不要继续将其当作用户当前要求或重复请求同一批准。

用户确认邮件已发；提供方尚未回复。API 可访问性、工程研究能力、合同许可是不同结论。允许真实数据在本地独立测试目录落盘、备份、恢复、断网重复分析及一次性数据库验证；不得因此把许可字段改成已确认。无需等待邮件才能开展这些已获用户批准的测试。没有授权采购、升级订阅、对外分发、接券商、main 合并或部署现有 staging/production。

## 可核验的测试结果

1. Basic 覆盖：AAPL、KO、历史 PARA；扩展 GOOG/GOOGL 股类与 BRK.B 点号代码。按实际响应报告身份、价格、币种、FIGI、公司行动缺口，不能只测一个热门大盘股。
2. 历史深度：近两年日线区间、明确超出两年的日期。403、空响应、部分历史或受限字段应分别记载；不得把更高档计划介绍当成本账户权限。
3. 分页、限速与采集恢复：真实有限分页；按 Basic 5 次/分钟控制，串行间隔至少 13 秒，遵守 Retry-After。不要通过故意超量请求压测服务；错误分支可使用本地受控响应验证。代表样本先充分验证，不因此抓取全市场数十年数据。
4. 本地持久化：真实响应和必要来源元数据存到仓库外专用目录，保留数据采集时间与请求；密钥不入仓库、响应存档、日志。普通快照完整性检查即可，无需新增事件哈希链。
5. 真正重放：退出采集进程后，用新进程、不读取 API 密钥、隔离该进程网络，读取落盘输入并重新计算，比较结果一致。不能仅调用内存对象后声称离线。
6. 备份恢复与坏数据：对独立测试副本做恢复、损坏检测；不破坏原始存档或既有数据库。验证保存机制不等于实测了数月自然时间，报告要写明实际间隔。
7. 研究链路：继续完成 C5 的 SEC 财务/身份与真实市场数据连接，在一次性测试库、离线文件和测试报告中检验；不要改变既有估值公式、行业映射、固定 cutoff 或 PARA 负例。若历史重建证据仍不足，应具体归因，不用“可保存”或“API 200”覆盖。
8. 给出 Basic 对当前个人日线研究的能力矩阵：已验证可用、缺口/限制、升级 Starter 能解决的项目、升级也不自动解决的历史语义/许可问题。测量调用数与耗时，说明 300 样本日更新和首次回填的吞吐估算是估算，不冒充已完成 300 股实际跑批。

## 外部独立探针

本线程正在 NUC 执行与 C5 业务代码分开的测试脚本 `/tmp/massive_basic_storage_probe_20260908.py`，原始数据目录：`/srv/cargoq/data/massive-basic-evaluation/20260908-owner-storage-test`。先等采集完成，勿同时使用同一密钥高频采集或改写该目录。脚本、manifest、结果和离线验证可作为接口/保存能力证据，不能直接替代 C5 生产代码集成验收。

## 历史语义保留

Massive ticker date 对 SEC 派生字段可能按报告期取值，仍需用 SEC 提交/公开时间校验。当前下载的历史重建与当年原始快照不能混称；缺失公开/修订时间保持未知。测试真实落盘和重放不要求把所有数据标为正式 accepted，也不能以全 synthetic 或全 rejected 宣称 C5 最终完成。

## 执行纪律

衔接当前 C5 修复结果，先实际运行定向测试及真实独立探针，再依据新缺陷修复；已经可用的 NUC Python/容器环境优先，不在缺 pytest 的角色环境里反复尝试失败安装。避免只迭代诊断脚本而迟迟不执行真实样本。现阶段不重复全量回归或独立审查；里程碑收尾仍按原工程纪律验收。
