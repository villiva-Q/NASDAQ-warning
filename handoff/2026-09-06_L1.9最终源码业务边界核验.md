# L1.9 最终源码业务边界核验

日期：2026-09-06。本文件记录项目规划所需的只读事实，不构成新一轮 L1.9 验收或全仓审查。

## 来源与版本

- 来源：NUC `/srv/git/value-investing-system.git`，通过既有 SSH 身份及 `git show` 读取确定提交对象；未修改远端文件或 ref。
- Commit：`ee8c8953a8538878dfef9a0944730cd669950f92`。
- 仓库路径：`apps/screener/l19_runtime.py`。
- 完整源码文件 SHA-256：`5b8cc8471b8b45ec1a55f46d4f36fcbd34682a9654015bca5f1c8141c3fae26c`。
- `git ls-remote` 回读：main=`3705e0be9e554b7535aedff524ea552d0a7ab499`，任务分支=`ee8c8953a8538878dfef9a0944730cd669950f92`。
- NUC `git merge-base --is-ancestor` 使用上述两个完整 SHA 执行，返回 0。

## 精确源码摘录

行号来自上述最终提交，为摘录辅助标记，不属于源码。

### 市场连接器及缓存（原文件第 658—675 行）

```text
 658     def _prepare_market_data(self) -> dict[str, object]:
 659         run, roster = self._run_and_roster()
 660         connector = ResilientMarketDataConnector(
 661             YahooFinanceChartConnector(archive=self.archive),
 662             StooqDailyConnector(archive=self.archive),
 663         )
 664         cutoff_day = self.config.point_in_time_cutoff.date()
 665         start = cutoff_day - timedelta(days=35)
 666         with self.connection.cursor(row_factory=dict_row) as cur:
 667             cur.execute(
 668                 "SELECT security_id,object_sha256,provider,identity_accepted,retry_count,error_category,coverage FROM scale_pilot_market_cache WHERE point_in_time_cutoff=%s",
 669                 (self.config.point_in_time_cutoff,),
 670             )
 671             cached_rows = {item["security_id"]: item for item in cur.fetchall()}
 672         payloads: dict[UUID, Any] = {}
 673 
 674         def cached(key: UUID):
 675             return payloads.get(key)
```

### 覆盖字段（原文件第 689—710 行）

```text
 689             coverage = {
 690                 "price": bool(payload and outcome.identity_accepted and payload.bars),
 691                 "volume": bool(
 692                     payload
 693                     and outcome.identity_accepted
 694                     and any(bar.volume is not None for bar in payload.bars)
 695                 ),
 696                 "corporate_actions": bool(
 697                     payload and outcome.identity_accepted and payload.actions_available
 698                 ),
 699                 "currency": bool(payload and outcome.identity_accepted and payload.currency),
 700                 "diluted_shares": False,
 701                 "market_cap": False,
 702                 "liquidity": False,
 703                 "adr_ratio": False,
 704                 "analyst_coverage": False,
 705                 "complete_delisting_history": False,
 706             }
 707             with self.connection.cursor() as cur:
 708                 cur.execute(
 709                     "INSERT INTO scale_pilot_market_cache (security_id,point_in_time_cutoff,object_sha256,provider,identity_accepted,retry_count,error_category,coverage) VALUES (%s,%s,%s,%s,%s,%s,%s,%s) ON CONFLICT (security_id,point_in_time_cutoff) DO NOTHING",
 710                     (
```

### continuation 与逐证券分类（原文件第 774—812 行）

```text
 774         if start.continuation:
 775             self._verify_continuation_results(run.run_id, roster, market)
 776             return {
 777                 "run_id": str(run.run_id),
 778                 "resumed": resume,
 779                 "idempotent": False,
 780                 "continuation": True,
 781                 "processed": len(roster),
 782                 "retries": sum(
 783                     int(market.get(member.security_id, {}).get("retry_count", 0))
 784                     for member in roster
 785                 ),
 786             }
 787         for member in roster:
 788             if member.security_id in done:
 789                 continue
 790             started = monotonic_ns()
 791             row = market.get(member.security_id)
 792             if member.classification == "unsupported":
 793                 status, classification, error = "unsupported", "unsupported", None
 794             elif not row or not row["identity_accepted"]:
 795                 status, classification, error = (
 796                     "repair",
 797                     "repair",
 798                     (row["error_category"] if row else "market_not_prepared"),
 799                 )
 800             else:
 801                 status, classification, error = "repair", "repair", "required_fundamentals_unknown"
 802             repository.record_result(
 803                 run_id=run.run_id,
 804                 security_id=member.security_id,
 805                 status=status,
 806                 classification=classification,
 807                 data_grade=None,
 808                 retries=(row["retry_count"] if row else 0),
 809                 elapsed_milliseconds=max(1, (monotonic_ns() - started + 999_999) // 1_000_000),
 810                 coverage=(row["coverage"] if row else {}),
 811                 error_category=error,
 812             )
```

## 可支持的结论

市场身份接受后的普通对象在本批量路径中仍被直接归为 `required_fundamentals_unknown`，结果统一使用 `data_grade=None`。市场 coverage 的股数、市值、流动性等字段在此直接标记为 False。这证明下一包需要处理基本面输入和筛选组件的批量接线；不能根据这些标记推断外部来源都缺少对应事实。

本核验不否定其他路径已实现的财务/评分组件，也不重新裁决历史测试、staging 健康、发布恢复或 L1.9 既有验收。
