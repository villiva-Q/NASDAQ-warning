# L1.8 阶段 B NUC 执行报告

状态：`PHASE_B_PASSED / LIMITED_REAL_SAMPLE_COMPLETE / CLAUDE_NOT_STARTED / NOT_PRODUCTION`

## Git 与制品门禁

- Bundle SHA-256：`5ffd800409540883bd9007b947ac571cdaf55c26307501af2b0f1d74e2e691ae`；`git bundle verify` 成功且历史完整。
- 隔离裸仓库核验：分支 `refs/heads/codex/l18-real-data-sample`，FULL_SHA `cd27e6648354944050d9de1ceada2cd8b6df6bdd`，TREE_SHA `c988bd718bc8b63b3d9632b8f097f29aaa8a6a5e`，父提交与 merge-base 均为 `7c36fa52055e3f6481bbba5fe1d2fc903f7d19b6`。
- `/srv/git/value-investing-system.git` 的任务分支已精确指向目标提交，按幂等规则未重复写 ref；main 未修改、未 force push。
- 恢复专项：14 passed；完整 PostgreSQL 回归：873 passed；`ruff check .`、format check、Compose 解析均通过。
- 初次全量测试容器因缺少 git/Compose v2 出现环境假阴性；补齐同版本工具后完整重跑通过，仓库代码未修改。
- 迁移头唯一为 `0010_l18_recovery_claim`；staging 已在该头，无待执行迁移，因此未重复备份或迁移。

## 镜像与部署

- 镜像标签：`value-investing-l1:cd27e6648354944050d9de1ceada2cd8b6df6bdd`
- 不可变引用 / image ID：`value-investing-l1@sha256:f924971d0df24a8cb831d8f21b4fe254d7a169715e03c6ee3cd0a98fc07c82a0`
- 平台：`linux/amd64`；容器入口冒烟与 Compose 解析通过。
- 仅滚动更新 control-api、scheduler、worker；PostgreSQL 未重建。新 release 缺少 observer-context bind 文件时 Compose 首次 fail-closed，随后从当前已部署 release 逐字节物化同一只读上下文，SHA-256 均为 `7e9c7495353113a1605f60ec972858b3f25b69624f872bd5c0de85b421e1b62b` 后重试成功；正式指针内容未改变。

## 孤立 run 恢复与制品策略

- 恢复前：run 1、result 0、market_observation 0、job 7、published pointer 1。
- 原 run `29c78bf7-e3ea-4301-a7c8-8e6c58d2d6b1`、screen_run `eb625f8b-3137-4455-b7db-933feade1a98`、输入哈希 `13c28b40ba3d6825cfc4ed8c772568c541c944c35f38333b6eb26987a3f3cb76` 被原位接管；未重新抓取。
- 旧根目录 cards 与新确定性 cards 相同，但旧 manifest/summary 字节哈希冲突，故旧文件原样保留；新输出旁路到 `attempts/attempt-000001-89fd79bc86ca3a63/`。数据库 manifest/summary 与 6 个解释卡均引用该 attempt。
- 恢复后：run 1、result 6、market_observation 11、job 7、published pointer 1；run/screen_run 身份未变。
- 首次恢复入口 4 秒返回 0。完全相同输入的重放及监控重放均返回 0；上述计数、主键范围、制品引用和既有发布指针不变，未创建重复 attempt。

## 六家公司结果

| Ticker | 形状 | hard gate / grade | 评分状态 | 最终分类 |
|---|---|---|---|---|
| KO | 美国盈利消费 | data fail / D | 不具备资格 | excluded |
| CAT | 周期资本密集 | repair_required / C | 双模型 repair_required | repair |
| SNOW | 高 SBC 软件 | data fail / D | 不具备资格 | excluded |
| PARA | 高杠杆敏感 | repair_required / C | 双模型 repair_required | repair |
| TSM | ADR/FPI/20-F | data fail / D | 不具备资格 | excluded |
| JPM | 不支持行业路由 | unsupported / unknown | 不评分 | repair/verify |

- 分类：candidate 0、repair 3、excluded 3；等级：C 2、D 3、unknown 1。
- 双模型 `V40_Q50`、`V50_Q40` 各有 2 条 `repair_required`；未为 unknown 或不支持对象填 0 或伪装通过。
- `candidate_manifest.v0.1` 保持容量语义：candidate 0、容量控制后的 repair_queue 1、excluded 3；summary 保留业务分类 repair 3。数据库、manifest、summary 与解释卡引用经逐项查询一致。

## 来源、覆盖与资源

- 归档响应：SEC companyfacts 6/6、SEC submissions 6/6、Yahoo chart 6/6；Stooq 0 次（无需降级）。所有归档记录持有对象哈希。
- 真实 close 与流动性观察覆盖 5/6；可形成 hard-gate market context 5/6，其中市值 1/6、平均日成交额 4/6、分析师覆盖 0/6（保持 unknown）。本批次持久化现金分红观察 1 条；公司行动库对样本保留现金分红记录，不推测缺失值。
- 监控重放峰值：PostgreSQL 5.42% CPU / 32.00 MiB；control-api 0.05% / 33.42 MiB；scheduler 0% / 36.95 MiB；worker 0.07% / 37.04 MiB。有限样本五个数据卷合计约 5.0 MiB。
- 一次性 run 容器执行过快，Docker stats 未稳定捕获其独立峰值；长期服务峰值与 Compose 限额无回归。

## 失败不发布与健康

- 可丢弃 PostgreSQL 的专项门禁覆盖：写制品后提交前崩溃、完整遗留采用、冲突旁路、残缺恢复、提交后异常、孤立接管、活跃竞争、双恢复者互斥、幂等和恢复失败不发布，14/14 通过。
- `test_recovery_failure_records_reason_and_never_publishes` 验证失败记录原因且不生成 candidate publication、不移动 pointer；真实 staging 的冲突窗口也曾 fail-closed，无伪成功。
- 最终 PostgreSQL、control-api、scheduler、worker 均 healthy；API `127.0.0.1:18080/health` 返回 ok。PostgreSQL 无公网暴露。

## 路径与限制

- Release：`/srv/value-investing/releases/cd27e6648354944050d9de1ceada2cd8b6df6bdd`
- Runtime 日志：`/srv/value-investing/runtime/l1-staging-cd27e6648354944050d9de1ceada2cd8b6df6bdd/`
- 结果卷逻辑根：`/var/lib/value-investing/l1/limited-sample/result/13c28b40ba3d6825cfc4ed8c772568c541c944c35f38333b6eb26987a3f3cb76/`
- 限制：仅六家公司、冻结 PIT；无完整退市历史，分析师覆盖仍 unknown；市值仅 1/6，流动性 4/6；不代表投资推荐，不是全市场、L2/L3 或 production。

未启动或派发 Claude 审查。
