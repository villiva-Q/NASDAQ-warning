# 新会话交接：C5 已完成，待确定集成与下一业务包

核验日期：2026-09-09，Australia/Melbourne。本文件用于接续本次长会话；以本文件的最新核验结果覆盖旧交接中的过期现状。此次只整理交接、读取远端状态和复制文档快照，没有启动新流水线、合并 main、部署或重跑测试。

## 1. 新会话先知道的结论

**C5 已按用户批准的 B「最新修订历史数据研究」口径完成验收，Co-op 已结束，成果已提交并推送到任务分支，工作树干净。尚未合并 main，尚未部署。**

- Co-op 任务：`L1.10C-5 SEC 补采、真实行情绑定与历史证据闭合`，ID `996cf51a64`，实际状态 `done`，最终管理阶段 `m108`。
- [打开当前 C5 Co-op 任务](https://villivanuc.tailee114c.ts.net:8443/?task=996cf51a64)。这是 Co-op 流水线，不是 Codex 桌面任务。
- 工程目录：NUC `/srv/cargoq/projects/value-investing-system-l110c5`。
- 分支：`codex/l110c5-real-market-data`。
- 实现提交：`2a1e91c7a3a746056da20e2250c7561957381848`。
- 文档回执提交 / 当前 HEAD：`f299f434ca9f3b19c495d27b5943cd44114b3e98`。
- 本次直接读取 origin 分支，回读同为 `f299f434ca9f3b19c495d27b5943cd44114b3e98`；`git status --short` 为空。
- origin/main 当前仍为 `3f33135a8f53a74cd9a1bf74e2ba056fac73c004`，包含 C4，不包含本次 C5 分支交付。
- 最新完整回归：**1242 passed，0 failures/errors/skipped，exit 0**；数据库迁移成功，实际编码用户 UID 1000，网络 `net:[4026531840]`。
- 固定容器验收：82 项受影响测试、3 项数据库测试通过；真实 AAPL 采用、持久化、幂等、独立恢复、断网重算通过。

不要再按上一轮聊天里的“1231 通过、2 项待修复”“Co-op 仍在复验”“C5 代码未提交”继续工作，这些状态已被上述结果替代。

## 2. 项目全貌与完成边界

目标是美股价值投资三层系统：L1 全市场筛选 → candidate manifest → L2 财报与价值研究 → research snapshot → L3 paper-only 决策工作台。

- L1 公共底座、规则组件、发布/恢复、有限样本入口已有阶段验收。L1.9 的 300 家 pilot 是历史已验证市场数据/批处理能力，不等于完整基本面评分已接通。
- C1/C2/C3 完成财务期间与 TTM、SEC 历史页采集、可配置样本/cutoff 的工程接线，均已集成。
- L1.11A 三层只读看板已交付并内部发布；L2/L3 页面只是规划分区，不代表正式业务实现。
- C4 历史市场证据处理、财务组合、重放/看板消费已验收并集成 main；当时固定真实市场样本仍均拒绝采用。
- 本会话 C5 推进了 SEC 实际补采、Massive Basic 真实接入、历史语义选择和生产/数据库/恢复/离线验收，现已形成真实 AAPL 研究采用正例。
- **C5 完成不等于完整财务筛选完成，不等于候选生成通过，也不等于全市场/L2/L3 完成。**

原工程总览见 [2026-09-08 工程计划与进度总览](2026-09-08_工程计划与进度总览.md)。其中 C5 未完成和 AAPL 尚未采用的旧状态由本文件更新；后续批量接线、股本语义等缺口仍需按代码核实，不应凭 C5 推断已解决。

## 3. 用户已作出的关键决定

### B 研究模式已明确批准

用户批准新增 `latest_revision_research`，对应 `l1.10c5.market-evidence.v5`。允许后来取得、无法证明 cutoff 前版本的真实历史值进入研究采用，并作为本轮 C5 真实生产/持久化/离线一致的正例。

- 固定样本与 cutoff 保留；不挪动 cutoff 求通过。
- `public_at/source_version/revision_basis` 未知仍未知，数据库为 SQL NULL。
- 保留实际 fetched_at、原件、证券身份、输入/run/rule 身份和修订信息缺口。
- 报告明确“不保证当时可知”。
- `strict_snapshot` 和有证据的 `historical_reconstruction` 保持独立判据；B 不能冒充严格 PIT 或历史版本证明。
- PARA 负例、财务规则和候选规则不放宽。

旧工单或旧主持人指令中“必须先补齐历史版本证据才允许 C5 正例”的要求，对已批准 B 不再适用。不要重新要求用户批准同一口径。

### Basic 实测与供应商回复

用户已授权真实响应本地保存、备份恢复、离线研究重算和一次性测试库验证。Massive 的 Cole 回复：**“Yes, this is a suitable use case for our stocks basic plan.”** 用户已提供截图并要求恢复 Co-op。

- 截图：NUC `docs/runbooks/evidence/massive-basic-support-reply-20260909.png`；本地 `handoff/evidence/massive-basic-support-reply-20260909.png`。
- 所述个人研究用途的适用性等待回函门槛已解除。不要继续等同一封邮件。
- 回复不证明历史价格版本/公开时间，也不推导其他用途、再分发或永久许可。
- 本会话没有新增采购或代发邮件。

### 权限与执行偏好

用户明确表示：**“如果需要，沙箱权限是可以开放的。”** 对必要的当前任务/项目沙箱权限已有授权，不为同一授权反复询问。选择能完成工作的配置，避免无差别改变所有项目或扩大数据库暴露。

用户希望主动完成已授权工作，讨厌重复确认、重复审计、无效测试和冗长交接。实际工具若拒绝操作，按规则处理；不能把口头授权冒充工具执行成功。

工程修复优先限当前项目；不擅改全局模型/推理配置。确定性顺序执行包使用既有 executor，用户指定 Luna low。业务判断与修复在适合的编码阶段完成；不要为了执行一个固定命令反复轮转角色。

## 4. 已验收的具体业务结果

| 样本 | 最新确认结果 |
|---|---|
| AAPL，2024-11-02T00:00:00Z | synthetic=false、latest_revision_research、accepted；真实 close 222.91；生产市场行、SQL NULL 来源缺口、幂等、恢复、断网 facts 一致已验证 |
| AAPL，2024-10-31T23:59:59Z | 早 cutoff 拒绝保留；SEC 披露过晚和未完成日线均有独立覆盖 |
| KO，2024-11-18T00:00:00Z | Basic 身份/日线接口样本有存档；没有验证该 cutoff 的生产采用，不能写成已接受 |
| PARA，2024-11-18T00:00:00Z | 原有 rejected 负例保留；接口取样成功不改变历史证据结论 |

财务仍为 `market_evidence_pending_financial_input`，没有证明财务筛选或候选通过。GOOG/GOOGL 同 CIK 但股类 FIGI 不同，不能仅按 CIK 绑定。单股日线与全市场日线的时间戳窗口语义不同，不能把其 t 当公开时间。

## 5. 测试、审查与证据位置

### 最终有效证据

- 完整回归：NUC `var/test-runs/20260909T031258.223381Z-2440968/summary.json`，1242/1242，524.15 秒，数据库开启、exit 0。本次回读 `var/test-runs/latest.json` 指向同一结果。
- 固定容器整包：`/srv/cargoq/data/l110c5-validation-20260909T032401Z`，s210。
- 其中正常步骤 exit 0；预期负例 `financial-negative.exit=2`、`corrupt-copy-rejected.exit=1`。本次直接读取所有 `.exit` 核对，不能把负例非零当整包失败。
- 首次正式 execute 必须 `replayed=false` 且 summary/manifest 位于正式 results；重试 `replayed=true` 并保持身份。此断言用于防止 pytest 残留运行污染正式验收。
- `primary.backup`、`recovery-receipts/restored-facts.json`、`isolated-output/facts.json` 位于上述整包证据目录。
- 0013→0011→0013 三场景迁移往返：项目 `artifacts/l110c5/s196-roundtrip-evidence/`；historical-reconstruction、latest-revision-research、release-only 均通过。实际 15 个文件，缺 `cleanup.command`；不要补造。
- s200 是本轮业务唯一独立审查；发现的 cutoff、未知交易所、看板契约、持久化缺陷经过后续修复和实测闭合，不因换会话启动第二次整轮审查。
- s214 提交/非强推/远端回读成功，m108 最终 finish。本次额外只读复核了 HEAD、工作树和远端分支一致性。

### 本地快照与过期文字提醒

已复制到新目录 `handoff/C5_2026-09-09最终快照/`，未覆盖旧交接：

- [最终 delivery 文档快照](C5_2026-09-09最终快照/L1.10C5_delivery.md)
- [最终 execution 文档快照](C5_2026-09-09最终快照/L1.10C5_execution.md)
- [1242 项通过机器摘要](C5_2026-09-09最终快照/summary.json)

**源码内 delivery/execution 顶部仍留有“推送回读和管理收尾待完成”的提交前文字。实际 s214、m108 和本次 Git 直接回读已经证明完成，不要再次提交、推送或重跑收尾来满足过期文字。** 新会话如需要精简文档，可在下一工作包做机械性同步；本次交接没有改动已交付远端分支。

旧本地 `handoff/L1.10C5_execution.md` 停留在早期连接修复时点，不应当作最新状态入口。

## 6. 编码角色数据库连接问题已经解决

根因已用实际 Codex sandbox 复现：普通用户宿主网络可连接项目 PostgreSQL，workspace-write 网络隔离内 TCP/Unix 返回 EPERM；测试库没有宕机。

项目修复已提交：

- `.codex/config.toml` 与 `.codex/rules/project-tests.rules`、`.codex/rules/coop-project-tests.rules`；实际内容以已交付文件为准。
- 固定绝对路径测试入口可按项目规则执行；不要改写成不匹配规则的临时 shell 包装。
- `scripts/test_project.py` 记录 effective_uid/network_namespace；root 调用 test 时降为项目普通用户；旧 root 所属回执权限已定点修复。
- s186 首次证明真实编码角色可以迁移并运行全量。后续最新 1242 项全绿进一步确认环境有效。
- 测试库按项目记录为 `l110c5-project-test-9895d08f01351aac`，loopback 10004；s210 明确未触碰它。本次未重新探测服务健康，后续仅在确需测试时检查。

固定命令（NUC）：

```sh
/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --database --note 本轮改动 tests/受影响测试.py
/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --full --note 本包收尾
```

受影响路径是示例占位，执行时替换。数据库迁移由入口处理。仅在有新的相关变更时重跑；换会话不构成重跑理由。Docker 生命周期、备份恢复按需由 executor 运行既有 `bash scripts/run_l110c5_acceptance.sh`，不新增 wrapper。

## 7. token 与协作效率的真实结论

已落实：固定 venv/测试入口、同包修复与测试、复用容器验收脚本、一份当前交接、历史按需读取、数据库连接不再强制中转 executor。

平台曾报告提示词从 1.06MB 到 39.8KB，减少 96.24%；这是输入字节变化，不能当成额度或 token 减少 96.24%。本会话还读取过实际阶段 CLI 的 `tokens used`：m89=53,702，m90=45,590，m91=49,501，s178=80,469；这些任务工作量不同，不能据此算优化比例。不要再宣称“没有 token 数据”，也不要凭 wrapper 未增加就宣称总消耗下降。

历史 commands.sh/container-wrapper.sh 数量在当时多次核验为 44/19 未增加；这是固定入口复用的证据，不是净 token 节省证据。本次未重新统计。长命令返回 session 后须继续等待到真实结束，不能把 30 秒工具返回当完成；历史曾有未完成测试目录，最终有效结果以上述 summary 为准。

## 8. 给平台搭建方的任务需求（已起草，尚未派发）

用户询问项目还是平台负责，已达成：当前项目修复保留，平台应将环境配置做成可复用能力。用户要求提供可派发文本，本会话已提供；**没有实际向平台搭建方发消息或新建平台任务。**

可直接派发：

> 请修复 Co-op 平台中“编码角色无法连接项目测试库而被迫转交 executor”的环境不一致，并将方案做成可复用项目配置。平台目录 `/srv/cargoq/ai-agents-coop`；参考验证项目 `/srv/cargoq/projects/value-investing-system-l110c5`。
>
> 复用本次实际根因与成功证据，检查工作目录、项目配置加载、沙箱权限和运行身份。让编码角色在同一工作包内完成修改、迁移、定向与全量测试；配置在新阶段和任务恢复后稳定生效，不依赖反复追加长提示词。统一测试回执归属和可读性，区分权限失败、数据库服务故障与业务断言失败。
>
> 用户已授权必要时开放沙箱权限；优先按项目/任务配置，不无差别放开全部项目，不改全局模型/推理设置、不扩大数据库暴露。保留 C5 已验证配置与成果，避免中断其他运行任务。日常数据库测试无需 executor 中转；Docker 生命周期/备份恢复可保持合理分工。
>
> 验收应从平台新启动编码阶段，直接完成数据库连接、迁移和测试，验证恢复/后续阶段及日志读取，并用另一可丢弃测试项目证明复用。不接触其他业务项目数据，不跳过测试、不放宽业务断言。交付实际修复、配置方法、适用范围、回滚和真实证据，说明 C5 哪些配置保留、哪些由平台承接。优先现有能力，不另建通用执行框架或每轮 wrapper；没有可比数据不报告 token 节省百分比。

## 9. 新会话的推荐续作顺序

1. 先读本文件，确认用户本轮要做的是 C5 集成、下一业务包还是平台改进；无需再排查已闭合 C5 环境问题。若距核验时间较久，仅只读查分支/任务有无新变化。
2. C5 工程下一候选动作是集成 main。此前本会话并未执行 C5 main 合并；用户对 C4 的“批准进入下一步”已经用于 C4，不能当成已经完成 C5 集成。本文件只是交接，不自动派发或实施合并/部署；若用户新指令授权，遵照当次指令推进。
3. 后续业务优先核实真实财务输入缺口、时点普通股本与 EPS 加权股数/稀释桥等语义，让少量真实样本完成财务+市场→硬门槛→评分→解释卡→candidate manifest。候选可以为零，但必须区分规则排除与输入缺失。不要为了候选数量填未知值。
4. 再将完整小样本链接入批量 runner，按 300 家完整业务验证后决定扩批；L1.9 原 pilot 不替代该项。KO 是否扩成研究采用正例应在下一包明确，不视作 C5 仍欠原验收。
5. L2/L3 正式业务仍未启动；按现行总控与候选输入约定推进。页面更新不能代替业务验收。
6. 平台任务可以单独派发，但当前项目无需等待平台改造。新会话未经用户实际派发要求，不声称平台已接单。

## 10. 操作入口与不应触碰的现场

- 本地文档项目：`/Users/mr.q/Obsidian/cargoQ/Projects/美股泡沫研究`。这是研究文档目录，不是 NUC C5 代码 checkout；同目录其他应用无关，不要误改。
- NUC SSH：`ssh -i /Users/mr.q/.ssh/villiva_nuc -o IdentitiesOnly=yes -o BatchMode=yes -o ConnectTimeout=8 -p 43382 root@192.168.50.232`。按工具权限执行；不要输出密钥、令牌或 DSN。代码/测试通常按 cargoq 运行。
- 远端 bare repo：`/srv/git/value-investing-system.git`；当前 origin 已配置，不改地址或认证。
- Co-op 平台：`/srv/cargoq/ai-agents-coop`；任务数据只读检查时在 `data/tasks.json` 精确过滤 `996cf51a64`，不要倾倒所有任务/完整历史。单阶段日志在 `data/996cf51a64/`，按需限量读取。
- Co-op 操作使用现有网页入口；若用户说新建 Co-op 流水线，不要误用 Codex 桌面的新任务工具。避免两个流水线同时写同一 checkout。
- 真实 Basic 原件只读保留：`/srv/cargoq/data/massive-basic-evaluation/20260908-owner-storage-test` 与 `20260908-owner-grouped-test`。原始数据、备份、var、.venv 不应强制加入 Git。
- 当前内部看板：[价值研究工作台](https://villivanuc.tailee114c.ts.net/value-research)。仍是先前 L1.11A 静态版本，不能声称它已经展示 C5 交付；本次没有更新发布路由或做服务健康检查。
- 实施前读远端 `AGENTS.md`、`docs/开发任务书/00B_内部项目最小防护与工程纪律-v1.0.md`；总控按远端生效 v0.2。已有用户授权与最新裁决优先，不把旧文件的过期状态当新阻塞。

## 给新会话的启动指令

请先阅读本交接，按其中 2026-09-09 的直接核验结果接续。C5 已验收、提交并推送，main 尚未集成，尚未部署。B 研究模式和必要的沙箱权限已有用户授权。保留已验证成果，避免重复测试、审查、采购询证和环境诊断；先结合我的新指令确定接下来推进 C5 集成、真实 L1 财务筛选纵切或平台环境配置改进，再完成对应工作。
