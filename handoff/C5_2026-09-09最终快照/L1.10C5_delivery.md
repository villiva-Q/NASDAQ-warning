# L1.10C5 当前交付说明

本工单受《内部项目最小防护与工程纪律 v1.0》约束。不得新增该文件第4节所列防护；除第5节停止条件外，不因范围、沙箱、审计轮次、测试环境或文件清单请求人工批准。

状态：**实现已提交；分支推送回读和最终管理收尾待完成。** 交付实现 `FULL_SHA`：`2a1e91c`（完整 SHA 以提交回执为准）。该提交包含已验收实现、测试、迁移、配置与交付文档。

## 已交付能力

- SEC 已使用真实 User-Agent 完成实际补采；复用历史 submissions 分页，修复失败/非成功缓存不得阻挡重采，并保留原始 body、headers、请求元数据、抓取时间和失败原件。真实 SEC 身份、CIK、accession、primary document、report/acceptance 时间和公开时间校验进入生产证据。历史 SEC 原件与解析/复验见 [execution 交接](L1.10C5_execution.md) 及 `artifacts/l110c5/sec-backfill/`。
- Massive Basic 已有真实身份、未复权日线和公司行动响应；AAPL 股息三页分页、近两年 500 条日线、403 历史范围和全市场日线接口均有独立存档。单股 `t` 是窗口起点、全市场 `t` 是窗口终点，均不作为公开时间。接口取样成功不等于生产采用；详见 [Basic 实测结果](L1.10C5_basic_storage_test_results_20260908.md) 与 [Basic 接口测试](L1.10C5_basic_interface_test_20260908.md)。
- 真实 provider binding 已绑定 SEC 身份、FIGI/股类、交易所、币种、报价归属、完成日线、成交量及公司行动输入；保留 `fetched_at`、原件、输入/run/rule 身份和来源引用。GOOG/GOOGL 同 CIK 但不同 share-class FIGI，不能只按 CIK 绑定。
- 保留三种独立历史语义：`strict_snapshot` 只接受原有严格快照；`historical_reconstruction` 只接受具备历史证据的重建；`latest_revision_research` 允许后来取得的真实历史值用于研究，但不保证当时可知。未知 `public_at`、`source_version`、`revision_basis` 不补值，持久化为 NULL，并在报告中明确限制。PARA 原有负例不改写。
- 看板展示历史模式和来源版本；`latest_revision_research` 明示“后来取得的历史修订值；不保证当时可知”。跨模式配对、输入 hash 或来源不一致继续拒绝。

## 固定样本结果

| 样本 / cutoff | SEC、身份与行情证据 | 生产采用状态 |
|---|---|---|
| AAPL / `2024-10-31T23:59:59Z` | SEC 披露时间晚于该 cutoff；11 月 1 日完成日线不能提前使用 | 已验证拒绝（early-cutoff 负例） |
| AAPL / `2024-11-02T00:00:00Z` | CIK `0000320193`、FIGI/股类、XNAS/NMS、USD、SEC 来源和 Massive 日线/行动原件均保留；真实 nominal close `222.91` | **已接受**；`synthetic=false`，`latest_revision_research`；`public_at/source_version/revision_basis` 未知，不能保证当时可知 |
| KO / `2024-11-18T00:00:00Z` | Basic 身份及 2024-11-14/15 日线接口样本已成功取得；未形成该 cutoff 的生产采用证据 | 未验证，不能推断已采用 |
| PARA / `2024-11-18T00:00:00Z` | Basic 身份及 2024-11-14/15 日线接口样本已取得；旧晚捕获/历史证据负例保持原状态 | 保留 rejected 负例；未验证生产采用 |

KO/PARA 的接口取样不替代固定 cutoff 的 SEC、行动、历史证据和生产采用链路。不得据此补表、改 cutoff 或推断采用。

## s210 验收证据

固定复验入口为 `bash scripts/run_l110c5_acceptance.sh`。s210 OUT 为 `/srv/cargoq/data/l110c5-validation-20260909T032401Z`，其中：

- `prepare.json`、`execute.json`、`before-retry.json`、`after-retry.json`：首次 execute `replayed=false`，重试幂等且身份一致；报告和 manifest 指向正式 `/evidence/research-work/results`。
- `recovery-receipts/restored-facts.json`、`primary.backup`：备份独立恢复后，AAPL 市场事实、close、average daily dollar volume、来源身份和 SQL NULL 缺口一致。
- `isolated-output/facts.json`：隔离网络的新进程离线重算一致。
- `financial-negative.exit` 为预期 `2`，`corrupt-copy-rejected.exit` 为预期 `1`；前者是财务输入未绑定/cutoff，后者是 checksum mismatch。财务仍为 `market_evidence_pending_financial_input`，不能宣称财务或候选通过。

固定包 affected JUnit 82 项、database JUnit 3 项均零失败/错误/跳过；完整回归依据 [`summary.json`](../../var/test-runs/20260909T031258.223381Z-2440968/summary.json) 为 1242 passed、exit 0，UID 1000、`net:[4026531840]`。s200 唯一独立审查发现的 cutoff、未知交易所、看板契约和行情持久化缺陷已由 s202/s204/s208 修复并由 s210 闭合；不表述为第二次审查。迁移往返复用 `artifacts/l110c5/s196-roundtrip-evidence`，三场景已完成；其原件实际 15 个文件且缺 `cleanup.command`，不补造。

Massive 的个人研究用途适用性已有回函记录于 [support reply evidence](evidence/massive-basic-support-reply-20260909.png)。该回函不证明历史价格版本/公开时间，也不扩大为其他用途或永久许可保证。

## 收尾

不得把 AAPL 的 `latest_revision_research` 采用写成 strict snapshot 或 historical reconstruction 的历史时点可知证明，也不得把财务负例改成候选通过。历史失败原件保留，正文只链接必要证据；不新增 wrapper、报告或业务判断。下一步仅为已授权的任务分支提交、非强制推送与回读。
