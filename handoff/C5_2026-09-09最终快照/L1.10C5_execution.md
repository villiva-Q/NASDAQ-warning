## 当前结论（文档收尾）

本工单受《内部项目最小防护与工程纪律 v1.0》约束；本轮仅更新交付说明和当前交接，未运行测试、迁移、Docker 或新审查。批准 B 口径的真实链路验收已通过：s210 已完成固定 AAPL `latest_revision_research` 的真实生产采用、持久化、备份恢复和断网离线一致性；KO/PARA 仍按既有证据状态记录，不能推断生产采用。下一步为已授权任务分支提交、非强制推送与回读；历史记录不代表仍需重跑。详见 [当前交付说明](L1.10C5_delivery.md)。

剩余任务：完成分支推送回读和最终管理收尾。实现 `FULL_SHA`：`2a1e91c`（完整 SHA 以提交回执为准）；本轮文档回执提交后记录最终 `DELIVERY_HEAD`。

## 2026-09-09 Luna 固定容器验收包（本轮通过）

本工单受《内部项目最小防护与工程纪律 v1.0》约束；本轮未新增防护、未修改代码/测试/迁移/固定脚本、未合 main、未部署；保留既有工作树改动。

- 分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`。固定命令 `bash scripts/run_l110c5_acceptance.sh` 单次持续等待至退出，exit `0`。OUT：`/srv/cargoq/data/l110c5-validation-20260909T032401Z`。
- affected JUnit `82 tests`、database JUnit `3 tests`，均 0 failures/errors/skipped；迁移、解析、materialize、prepare、生产 SQL 事实、重试、备份/独立恢复、恢复事实、断网新进程重算均 exit `0`。首次正式 execute `replayed=false`，summary/manifest 均在正式 `/evidence/research-work/results` 且可读；重试 `replayed=true` 且身份一致。
- 固定 AAPL cutoff `2024-11-02T00:00:00Z`：`synthetic=false`、`latest_revision_research`、adoption `accepted`；生产/恢复 `market_observation` 保留真实 close 与 `average_daily_dollar_volume`，`source_public_at/source_version/revision_basis` 为 SQL NULL。来源原件、fetched_at、SEC 身份、run/input/rule 身份及“不保证当时可知”限制均保留。断网 facts 与恢复事实一致。
- 预期负例通过：`financial-negative.exit=2`、`corrupt-copy-rejected.exit=1`；其余步骤均为 `0`。本包容器已清理；既有 `l110c5-project-test-9895d08f01351aac` 未触碰。`git diff --check` exit `0`。
- 机器证据及各步骤 command/stdout/stderr/exit 均在 OUT，包含 `affected.junit.xml`、`database.junit.xml`、`prepare.json`、`execute.json`、首次回执检查、重试、恢复、离线 facts。下一步由管理者决定交付文档与任务分支提交收尾；本记录不自行认定 C5 完成。

## 2026-09-09 真实归档测试状态污染修复（编码阶段完成）

本工单受《内部项目最小防护与工程纪律 v1.0》约束；未新增第4节防护，未启动 Docker 生命周期、未重采集、不读密钥、未合 main、未部署。分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`；保留全部既有工作树改动。

- 根因：`tests/test_l110c5_database_archive.py` 两个真实归档测试自行 `psycopg.connect()`，未使用 `tests/conftest.py` 的 `database_connection` 隔离 fixture。`LimitedSampleRunner` 正常内部提交成功运行；测试结束仅关闭连接，故同一一次性库后续正式 execute 命中该 input hash 并返回 `replayed=true`，其 summary/manifest 指向 pytest 临时 results，容器正式阶段不可读取。
- 修复：两个归档测试接入 `database_connection`；fixture 除 setup 外也在 teardown 清空经验证的一次性库。若故障注入测试已关闭原连接，teardown 重新验证并连接同一 disposable DSN 后清理，保持这些测试的关闭连接语义。新增真实 PostgreSQL 回归：验证前序归档测试清理后的 `limited_sample_run=0`，不同正式 results 根首次 execute 为 `replayed=false` 且 summary/manifest 位于该根并可读，第二次为 `replayed=true` 且 run/input 身份相同。真实 adopted close、average_daily_dollar_volume 与 SQL NULL `source_public_at/source_version/revision_basis` 断言保留。
- `scripts/run_l110c5_acceptance.sh` 在首次正式 execute 后、baseline 前新增实际回执检查：必须 `replayed=false`，且 summary/manifest 为正式 `/evidence/research-work/results` 内可读普通文件；重试、恢复、SQL 和负例契约未变。
- 定向真实 PostgreSQL：`... test --database --note '修复真实归档测试状态污染' tests/test_l110c5_database_archive.py tests/test_l17_b3b_postgres.py` exit `0`，9 passed、0 failures/errors/skipped；迁移 exit `0`。机器回执/JUnit：`var/test-runs/20260909T031206.789958Z-2435003/summary.json`，环境 `effective_uid=1000`、`network_namespace=net:[4026531840]`。
- 收尾全量：`... test --full --note '真实归档测试隔离修复收尾'` exit `0`，迁移 exit `0`、1242 passed、0 failures/errors/skipped、524.15 秒。机器回执/JUnit：`var/test-runs/20260909T031258.223381Z-2440968/summary.json`，同为 UID 1000 / `net:[4026531840]`。`git diff --check` exit `0`。
- 下一步：固定容器入口已就绪，由管理者派既有 Luna low executor 以全新 OUT 运行 `bash scripts/run_l110c5_acceptance.sh`；首次正式 execute 将在 baseline 前先拒绝本轮已复现的污染。该容器验收尚未在本编码阶段重跑，不能据此认定 B/C5 完成。

## 2026-09-09 Luna 固定容器验收包（本轮停止）

本工单受《内部项目最小防护与工程纪律 v1.0》约束；本轮未新增防护、未修改代码/测试/入口脚本/既有归档，未合 main、未部署。

- 分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`；执行前已有未提交工作树，原样保留。
- 固定命令 `bash scripts/run_l110c5_acceptance.sh`，exit `1`。OUT：`/srv/cargoq/data/l110c5-validation-20260909T024839Z`。
- 已执行并 exit 0：镜像/源码核验、affected 测试、六样本解析、一次性库迁移与数据库测试、materialize、prepare、execute。JUnit：`affected.junit.xml` 82 tests/0 failures/0 errors/0 skipped；`database.junit.xml` 2/0/0/0。
- 首个非预期失败：`pre-retry-baseline.exit=1`。错误为 `summary report is not a regular file: /l110c4-pytest-tmp/c5-db/test_real_archive_financial_ga0/run/results/.../summary.json`。因此按依赖停止，未执行 retry-assert、备份/恢复、恢复 SQL、隔离网络重算、损坏副本拒绝及后续清理核验；不据此宣称本轮生产/恢复验收或 C5 完成。
- 本轮无可报告的 prepare/execute SQL 断言、重试/恢复/离线 facts 或负例回执；`prepare.json`、`execute.json` 已保留。脚本创建的数据库容器已不存在（按 ID 核验未找到）；既有 `l110c5-project-test-9895d08f01351aac` 未触碰。OUT 证据可由普通项目用户读取，`git diff --check` exit 0。

# C5 当前执行交接

## 2026-09-09：s202 编码修复包（当前状态）

本工单受《内部项目最小防护与工程纪律 v1.0》约束；未新增第4节防护，未启动 Docker 生命周期、未改平台/全局配置、未合 main、未部署。当前分支 `codex/l110c5-real-market-data`，HEAD 仍为 `02b6e23866bfdeb111e118c5b47112421927ea36`；所有既有已跟踪和未跟踪 C5 改动保留。

- 已确认 s200 遗留全量目录 `var/test-runs/20260909T022413.903186Z-2349130/` 没有存活 pytest/test_project 进程，只有约 29% pytest 进度、无 JUnit/summary。它是旧调用在约 30 秒返回后未持续等待所留的未完成运行，不是通过或项目/数据库断言失败；旧目录的迁移回执不作为本轮结果。
- 定点补充真实归档覆盖：`packages/connectors/market_evidence.py` 将日线完成时间选择抽为生产使用的 `_latest_completed_daily_bar`。`tests/test_massive_basic.py` 从只读 AAPL 真实归档的临时派生副本只保留 2024-11-01 日线，固定早 cutoff `2024-10-31T23:59:59Z` 时精确拒绝“no completed daily bar at cutoff”。该覆盖与既有 SEC 披露过晚断言相互独立；没有改原归档、cutoff、2024-11-02 正例或 XXXX 负例。
- 编码角色定向命令 `/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --note 's202 real archive daily completion cutoff regression' tests/test_massive_basic.py` exit 0：46 passed、0 failures/errors/skipped；summary/JUnit 为 `var/test-runs/20260909T023054.107694Z-2363215/`。
- 同一编码角色固定全量命令 `/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --full --note s200-review-fixes` 持续等待至真实退出；迁移 exit 0、pytest exit 0，1241 passed、0 failures/errors/skipped、369.26 秒。summary/JUnit 为 `var/test-runs/20260909T023113.660017Z-2363750/`，`var/test-runs/latest.json` 已指向它；环境 `effective_uid=1000`、`network_namespace=net:[4026531840]`。
- 已核对下一包固定入口 `bash scripts/run_l110c5_acceptance.sh`：输入为只读 `STORAGE=/srv/cargoq/data/massive-basic-evaluation/20260908-owner-storage-test`、`GROUPED=/srv/cargoq/data/massive-basic-evaluation/20260908-owner-grouped-test`、现有 AAPL SEC manifest 与 `BAD_FIN`。它按顺序执行离线解析、一次性库迁移/生产 execute、`assert_l110c5_archive_acceptance.py` 的 accepted 市场观察与 SQL NULL provenance 查询、幂等、备份独立恢复（再次查询同一 `market_observation`）、断网重算及损坏副本预期拒绝。无需另造 wrapper；本阶段未运行该 Docker 包。预期负例：`financial-negative` exit 2、`corrupt-copy-rejected` exit 1；其余任一非零均为首个非预期失败，停止依赖步骤并保留该 OUT 的 command/stdout/stderr/exit。
- s196 三场景迁移往返证据继续复用，不额外重跑。B 的 `latest_revision_research` 已获批准，真实 AAPL 采用仍明确不保证当时可知，`public_at/source_version/revision_basis` 必须保持未知/SQL NULL；strict、historical reconstruction、PARA 负例及财务失败不放宽。下一步仅交既有 Luna low executor 执行上述固定包。

以下历史段均为已被上述 s202 事实替代的过程记录；尤其任何称 B 必须先补齐历史价格版本/公开时间证据才可推进的旧表述不再适用，不能覆盖已批准的 `latest_revision_research` 边界。

## 2026-09-09：s196 原件可读交付副本

本工单受《内部项目最小防护与工程纪律 v1.0》约束；仅复制既有证据，未改原件权限、业务代码或测试。

- 分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`。
- 原件：`/srv/cargoq/data/l110c5-roundtrip-WB2PUtiG`；交付副本：`artifacts/l110c5/s196-roundtrip-evidence/`。实际复制 15 个 `command/stdout/stderr/exit` 普通文件，缺 `cleanup.command`；副本目录 0755、文件 0644，原件未变。
- 副本与原件逐文件 `cmp` exit 0。实际退出码：`db-start=0`、`db-ready=0`、`roundtrip=0`、`cleanup=0`。`roundtrip.stdout` 核实三场景 `historical-reconstruction`、`latest-revision-research`、`release-only` 均 complete，且 `0013_l110c5_latest_revision -> 0011_l19_scale_pilot -> 0013_l110c5_latest_revision` 标识存在。
- 清理记录在副本 `cleanup.stdout`、`cleanup.stderr`、`cleanup.exit`；stdout 留存容器标识，cleanup exit 0。
- 普通项目用户 `cargoq` 通过 `su` 只读列举实际 15 个文件并读取关键 stdout，`MARKERS=3`；缺 `cleanup.command` 未补造。首次尝试 `runuser` 因命令不存在 exit 127，未影响后续独立验证；未改用户或宿主配置。
- 未补生成缺失记录；本交付不重跑数据库生命周期，不据此宣称独立审查或 C5 最终完成。

## 2026-09-09：0013 迁移往返容器续跑完成

本工单受《内部项目最小防护与工程纪律 v1.0》约束；未新增第4节防护。按既有续跑包执行，未改业务代码、测试、迁移、宿主权限或平台配置，未合 main、未部署。

- 分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`；工作树既有未提交改动保留。镜像 ID：history-fix `sha256:56e150495a2c106e16550de1dca34369c8fc78595e7aa64ea9f49e9b47dd2dd7`，PostgreSQL `sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685`。
- 本次 OUT：`/srv/cargoq/data/l110c5-roundtrip-WB2PUtiG`。`db-start=0`、`db-ready=0`、`roundtrip=0`、`cleanup=0`；roundtrip 实际容器 ID 已在 `db-start.stdout` 留存，清理 stdout/stderr/exit 已留存，按名称核对无本次容器残留。命令参数完整保留并脱敏口令，无完整 DSN 输出。
- `roundtrip.stdout` 明确包含 `historical-reconstruction`、`latest-revision-research`、`release-only` 三项 scenario complete，以及 `migration roundtrip: 0013_l110c5_latest_revision -> 0011_l19_scale_pilot -> 0013_l110c5_latest_revision`。
- 本轮证据：OUT 内各步骤 `command/stdout/stderr/exit`；`git diff --check=0`。未重跑编码测试、全量回归、采集或 acceptance 整包；不据此认定独立审查或 C5 最终完成，既有 AAPL 研究正例及其恢复/离线证据沿用 s188/s186 记录。

## 2026-09-09：0013 迁移往返场景隔离修复（编码阶段完成）

本工单受《内部项目最小防护与工程纪律 v1.0》约束；未新增第4节防护。分支 `codex/l110c5-real-market-data`、HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`，保留既有未提交 C5 改动；未合 main、未部署、未操作 Docker 生命周期、未改宿主权限或平台配置。

- 定点修复 `scripts/l110c4_disposable_migration_roundtrip.py`：顺序改为 historical-reconstruction、latest-revision-research、release-only。场景之间仅删除脚本创建的行情行；release 根最后创建并保留至一次性容器销毁。未使用 `DELETE/UPDATE/TRUNCATE` 操作 release、未禁用触发器或修改迁移。每一场景输出完成标识，最终成功标识仍只在全部检查后输出。
- `tests/test_migrations.py` 新增真实 PostgreSQL 完整入口回归，直接以受验证 `database_url` 启动该脚本，检查三项场景标识、`0013 → 0011 → 0013` 成功、最终 revision 与最终 release 根存在；不 mock 迁移或 SQL。该回归覆盖了修复前会在 release 根删除处失败的完整顺序。
- 编码角色实际命令：`/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --database --note '修复往返场景顺序并覆盖完整入口' tests/test_migrations.py`，exit `0`。JUnit：`var/test-runs/20260909T020235.090686Z-2307784/junit.xml`；summary：`var/test-runs/20260909T020235.090686Z-2307784/summary.json`；迁移 `migrate.exit=0`。机器统计为 7 passed、0 failures/errors/skipped、30.28 秒，`effective_uid=1000`、`network_namespace=net:[4026531840]`。
- `ruff check scripts/l110c4_disposable_migration_roundtrip.py tests/test_migrations.py` 与 `git diff --check` 均 exit `0`。仍待管理者后续派 Luna low 执行容器续跑；本轮不认定独立审查或 C5 最终完成。

## 2026-09-09：0013 迁移往返续跑（阻断，待定点修复）

本工单受《内部项目最小防护与工程纪律 v1.0》约束；未新增第4节防护。按既定续跑包执行，未修改业务代码、测试、迁移、平台配置、宿主权限或 main，未部署；既有未提交改动保留。

- 输入：分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`；history-fix 镜像 ID `sha256:56e150495a2c106e16550de1dca34369c8fc78595e7aa64ea9f49e9b47dd2dd7`，`postgres:16-alpine` ID `sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685`。工作树执行前已有未提交改动，未清理或覆盖。
- 实际 OUT：`/srv/cargoq/data/l110c5-roundtrip-r20260909153000rt`。后缀为本次新建，未覆盖历史 OUT；数据库/角色为 `vis_l17_test_db_r20260909153000rt` / `vis_l17_test_role_r20260909153000rt`，命令证据已脱敏口令且保留完整实际参数。
- 步骤结果：`db-start=0`，容器 ID `9c3d6a5f98473c231824a076b95a5fda85ae9e2c3dd1fa623e536f744ab2faf7`；`db-ready=0`；`roundtrip=1`。各步骤 `command/stdout/stderr/exit` 均在 OUT 内。
- 首个非预期错误：roundtrip stderr 显示脚本在 `scripts/l110c4_disposable_migration_roundtrip.py:156` 清理 `DELETE FROM release_execution` 时触发既有约束 `l17_release_execution_immutable()`，错误为 `release execution roots are immutable`。迁移往返未完成，未执行或宣称三种拒绝场景及 `0013 → 0011 → 0013` 通过。
- 清理：本次成功创建的容器已由 trap 清理；`docker ps -a --filter name=l110c5-roundtrip-r20260909153000rt` 无输出，匿名卷随 `docker rm -fv` 处理。未触碰其他容器或卷。OUT 原件保留。
- 收尾：本轮未改代码、未重试、未运行全量回归。剩余事项是针对上述 immutable-root 清理语义定点修复后，由管理者重新派发续跑；不据此认定独立审查或 C5 最终完成。


## 2026-09-09：0013 迁移往返定点修复（待 Luna low 续跑）

本工单受《内部项目最小防护与工程纪律 v1.0》约束；未新增第4节防护。分支仍为 `codex/l110c5-real-market-data`、HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`，保留既有未提交 C5 改动；未合 main、未部署、未改宿主权限或平台配置。

- 读取 s188 OUT 的 `roundtrip.{command,stdout,stderr,exit}` 后确认：失败发生在 `disposable_database_target(dsn)`，迁移没有开始；历史 `roundtrip.command` 使用 `<container>`、`<db>`、`...` 占位而不能证明 URL。stderr 的精确错误为 `TEST_DATABASE_URL has unsupported connection controls`，不是数据库宕机。Co-op `s188.run1.log` 仅能确认同一错误，未包含可安全还原的完整参数，故未猜测原 URL。
- 修复 `scripts/l110c4_disposable_migration_roundtrip.py`：三种数据状态分别精确匹配真实迁移拒绝信息。历史重建保留 0012 的 backup 拒绝；仅含 `target_alembic_head=0013_l110c5_latest_revision` 的 release 断言 0013 release 拒绝；独立 `latest_revision_research` 行断言 0013 provenance+backup 拒绝。每种拒绝后均核验 revision、数据、NULL `source_public_at/source_version/revision_basis` 缺口和相关列/约束不变，场景之间清除会遮蔽后续 guard 的数据。
- 新增 `tests/test_migrations.py` 的真实 PostgreSQL 覆盖：分别精确验证 0013 的 release-only 与 latest-revision 数据拒绝及不变性，未改迁移业务语义、strict、historical_reconstruction、PARA 或投资规则。
- 编码角色实测命令：`/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --database --note '修复0013迁移往返场景' tests/test_migrations.py`，exit `0`，6 passed、0 failures/errors/skipped；JUnit `var/test-runs/20260909T015415.039381Z-2292212/junit.xml`，机器统计 `var/test-runs/20260909T015415.039381Z-2292212/summary.json`（effective_uid `1000`、host network）。`ruff check` 与 `git diff --check` 均 exit `0`。

以下是既有 Luna low executor 可独立运行的一次性续跑包。它只新建并清理本次容器及匿名卷、无宿主端口、使用合法 16 位小写字母数字后缀和容器内 `5432`；首个非预期失败立即退出。将产生的实际 `roundtrip.command` 保留所有参数，但将口令替换为 `[redacted]`，不使用省略号。新 OUT 必须不存在；不得覆盖 s188 原件，也不得对已采用/恢复库运行。

```bash
set -euo pipefail
ROOT=/srv/cargoq/projects/value-investing-system-l110c5
IMAGE=l110c5-history-fix:validation-entry-repair-20260908t073726z
POSTGRES_IMAGE=postgres:16-alpine
OUT=$(mktemp -d /srv/cargoq/data/l110c5-roundtrip-XXXXXXXX)
SUFFIX=$(od -An -N16 -tx1 /dev/urandom | tr -d ' \n')
DB_NAME=vis_l17_test_db_$SUFFIX
ROLE=vis_l17_test_role_$SUFFIX
PASS=$(od -An -N16 -tx1 /dev/urandom | tr -d ' \n')
DSN=postgresql://$ROLE:$PASS@127.0.0.1:5432/$DB_NAME
DB=l110c5-roundtrip-$SUFFIX
CONTAINER_ID=
cleanup(){ cleanup_rc=0; if [[ -n "$CONTAINER_ID" ]]; then docker rm -fv "$CONTAINER_ID" >"$OUT/cleanup.stdout" 2>"$OUT/cleanup.stderr" || cleanup_rc=$?; fi; printf '%s\n' "$cleanup_rc" >"$OUT/cleanup.exit"; }
trap cleanup EXIT
run(){ name=$1; shift; printf '%q ' "$@" | sed "s/$PASS/[redacted]/g" >"$OUT/$name.command"; printf '\n' >>"$OUT/$name.command"; set +e; "$@" >"$OUT/$name.stdout" 2>"$OUT/$name.stderr"; rc=$?; set -e; printf '%s\n' "$rc" >"$OUT/$name.exit"; return "$rc"; }
run db-start docker run -d --name "$DB" --memory=512m --cpus=.75 --pids-limit=128 -e POSTGRES_DB="$DB_NAME" -e POSTGRES_USER="$ROLE" -e POSTGRES_PASSWORD="$PASS" "$POSTGRES_IMAGE" || exit $?
CONTAINER_ID=$(tr -d '\r\n' <"$OUT/db-start.stdout")
ready(){ i=0; until docker exec "$DB" pg_isready -h 127.0.0.1 -U "$ROLE" -d "$DB_NAME" >/dev/null 2>&1; do i=$((i+1)); test "$i" -lt 30 || return 1; sleep 1; done; }
run db-ready ready || exit $?
run roundtrip docker run --rm --network="container:$DB" --memory=768m --cpus=1 --pids-limit=192 --read-only --tmpfs /tmp:rw,noexec,nosuid,size=384m --tmpfs /workspace:rw,nosuid,nodev,exec,size=512m --tmpfs /l110c4-pytest-tmp:rw,nosuid,nodev,exec,size=256m -e TMPDIR=/l110c4-pytest-tmp -e TEST_DATABASE_URL="$DSN" -v "$ROOT:/source:ro" -v "$OUT:/evidence:rw" -w /source --entrypoint bash "$IMAGE" /source/scripts/l110c4_nuc_container_entry.sh /opt/l110c5-venv/bin/python scripts/l110c4_disposable_migration_roundtrip.py || exit $?
```

该续跑成功才可将 `0013 → 0011 → 0013` 往返标为通过；本轮未运行 Docker、未宣称独立审查或 C5 完成。

## 2026-09-09 Luna B 真实研究验收包

本工单受《内部项目最小防护与工程纪律 v1.0》约束；未新增第4节防护。按既定范围执行，未改业务代码、测试、入口脚本、平台配置、宿主权限、main 或部署；既有未提交改动保留。

- 分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`；工作树执行前已有未提交改动。
- 固定命令 `bash scripts/run_l110c5_acceptance.sh` exit 0；实际 OUT：`/srv/cargoq/data/l110c5-validation-20260909T014554Z`。六样本解析、0013 迁移/数据库测试、AAPL `latest_revision_research` production prepare/execute、accepted、持久化、幂等重试、财务负例、备份恢复、断网新进程重算、损坏副本拒绝均按脚本判据通过；负例 `financial-negative.exit=2`、`corrupt-copy-rejected.exit=1` 为预期结果。
- 真实采用正例：AAPL，cutoff `2024-11-02T00:00:00Z`，`synthetic=false`、adoption `accepted`；run/input snapshot/rule version、来源原件与 fetched_at、价格/成交量/币种/证券归属及 NULL 缺口均保留。恢复后事实与数据库计数一致（9 sources、1 result、1 run），离线新进程 facts 一致。该模式明确不保证当时可知；`public_at`、`source_version`、`revision_basis` 未知保持 NULL。KO 在六样本 grouped 解析中未保留为短样本；其余状态见 `six-sample.json`。
- 必要迁移往返已执行于独立临时 PostgreSQL 容器（无宿主端口），但 exit 1，停止后续依赖步骤。首个具体错误：`tests/conftest.py` 拒绝 `TEST_DATABASE_URL` 的连接控制（`DisposableDatabaseSafetyError: TEST_DATABASE_URL has unsupported connection controls`），原始证据：`roundtrip.command`、`roundtrip.stdout`、`roundtrip.stderr`、`roundtrip.exit`。因此不能写成 `0013→0011→0013` 往返通过；临时容器已清理。
- 关键证据：OUT 内 `database.junit.xml`、`affected.junit.xml`、`prepare.json`、`execute.json`、`after-retry.json`、`recovery-receipts/restored-facts.json`、`isolated-output/facts.json`、各步骤 command/stdout/stderr/exit。`var/test-runs/latest.json` 仍指向既有 s186 全量 1233/1233，不冒充本轮容器统计。`git diff --check` 通过。
- 未执行/未完成：因迁移往返首个非预期失败，未宣称往返通过、独立审查通过或 C5 最终完成；未修改实现或绕过断言。

## 2026-09-09 编码角色实测、0013 迁移契约修复与 B 验收交接

本工单受《内部项目最小防护与工程纪律 v1.0》约束；未新增第4节防护，未改平台/全局设置、宿主权限、main 或部署。当前分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`；保留既有未提交 C5 工作树改动。

新编码角色直接执行（无 executor 中转）：

`/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --full --note coding-role-db-access-regression`

首轮迁移 `migrate.exit=0`、pytest exit `1`：1233 项中 1231 passed、2 failures、0 errors、0 skipped；证据 `var/test-runs/20260909T012925.321079Z-2242715/`（`summary.json`、`junit.xml`、`migrate.{stdout,stderr}.log`、`pytest.{stdout,stderr}.log`）。失败仅为 `tests/test_l17_cli_contract.py` 未把 B 的真实线性迁移 `0012 → 0013` 纳入 fixed graph/source-hash 契约。

同一编码工作包修复该测试：base 路径及从 `0011` 的路径包含 `0012_l110c5_market_history`，受信任来源集合包含 `0013_l110c5_latest_revision`，哈希断言改校验 `0013_l110c5_latest_revision_research.py`。未改变 B、strict/PARA/PIT 或生产代码语义。定向命令

`/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --note '同步0013迁移图契约' tests/test_l17_cli_contract.py`

exit `0`，178/178，证据 `var/test-runs/20260909T013613.590260Z-2255642/`。因修复改变工作树，重新执行完整固定入口：

`/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --full --note coding-role-db-access-regression-after-0013-contract-fix`

exit `0`；迁移成功，1233/1233 passed、0 failures/errors/skipped，375.25 秒。机器统计指针 `var/test-runs/latest.json` 与 `var/test-runs/20260909T013620.498558Z-2255867/summary.json`；同目录保存 JUnit、stdout/stderr 与命令回执。`environment.effective_uid=1000`，`network_namespace=net:[4026531840]`，故本编码角色确以普通项目用户和已验证宿主网络路径完成数据库迁移与全量测试，不是 root 或仅退出码结论。

下一阶段仅交既有 Luna low executor 运行固定 `bash scripts/run_l110c5_acceptance.sh`（不在本编码阶段执行 Docker）。入口已固定使用只读 `STORAGE=/srv/cargoq/data/massive-basic-evaluation/20260908-owner-storage-test`、`GROUPED=/srv/cargoq/data/massive-basic-evaluation/20260908-owner-grouped-test`、现有 AAPL SEC manifest 与固定 financial negative input；新 OUT 必须不存在，首个非预期失败停止，记录 command/stdout/stderr/exit。执行目标依次为：离线六样本解析、一次性 PostgreSQL迁移和数据库测试、AAPL `latest_revision_research` 的 production prepare/execute、`synthetic=false`/accepted 及 public_at/source_version/revision NULL 缺口断言、重试幂等、备份独立恢复、断网新进程重算、损坏副本拒绝。使用 `artifacts/l110c5/sec-backfill/verify-20260909-dei-date-normalization/` 的 `content_accepted` SEC 原件作为验收时的只读交叉材料；它不替代现有 AAPL SEC manifest，也不补造行情 public time/version/revision。该固定包的实际运行、Docker 生命周期、迁移往返、真实采用和恢复仍未在本阶段运行；不得据此宣称 B 或 C5 已完成。

## 编码角色数据库连接修复（角色实测已通过）

已复现根因：同一 cargoq uid1000 在宿主可连接项目一次性 PostgreSQL，Codex workspace-write 网络隔离内 TCP/Unix 均 EPERM。测试库未宕机。

项目 .codex/rules/project-tests.rules 使用原生精确前缀规则，仅允许绝对项目 .venv/bin/python + 绝对 scripts/test_project.py + test 入口以普通用户在沙箱外执行；项目 .codex/config.toml 只有说明注释，项目原已受信任，未改全局或 Co-op 配置。规则限制命令入口，不是测试代码的网络目的地过滤。任意 Python、Docker、db-start/db-stop 未获此规则放行。官方依据：https://learn.chatgpt.com/docs/agent-configuration/rules 。

scripts/test_project.py 记录运行 UID 与网络命名空间；root 调用 test 时在锁与迁移前降为项目属主。19 个旧 root 归属测试回执条目已修复。初测数据库 1/1 通过，证据 var/test-runs/20260909T012334.322772Z-2232936；静态检查、规则正反例与环境修复一次独立审查已通过。

2026-09-09，实际 Co-op 新编码阶段 s186「编码环境验收」直接执行以下命令，无 executor 中转：
/srv/cargoq/projects/value-investing-system-l110c5/.venv/bin/python /srv/cargoq/projects/value-investing-system-l110c5/scripts/test_project.py test --full --note coding-role-db-access-regression

机器回执 var/test-runs/20260909T012925.321079Z-2242715/summary.json：environment.effective_uid=1000、network_namespace=net:[4026531840]，迁移 migrate.exit=0；1233 tests，1231 passed、2 failures、0 errors、0 skipped，371.25 秒，pytest exit=1。回执归普通用户所有。原数据库连接障碍已在真实编码角色中消除，不能将本轮完整回归写成全绿。

剩余两项为 tests/test_l17_cli_contract.py 的固定迁移图/版本集合断言，与 B 新增 0013 迁移有关；已由 s186 同一编码工作包继续修复并通过固定入口复验，不转给 executor 重跑连接。后续实际结果由该编码阶段更新本文件。Docker 备份恢复等生命周期步骤仍交既有 executor，属于正常角色分工；C5 B 业务最终验收尚未完成。

## 当前用户裁决：B 已批准

用户已明确批准另设“最新修订历史数据研究”模式，并将 C5 真实正例验收调整为该模式的生产执行、持久化及离线一致。允许使用后来取得、无法证明 cutoff 前版本的历史值；保留固定样本与 cutoff，未知 public_at 保持未知，记录真实抓取时间、来源和修订依据缺口，明确报告“不保证当时可知”。原 strict、有证据历史重建和 PARA 负例保持独立及原有规则。该裁决已填入“历史口径裁决”关卡并点击批准，页面确认管理决策92恢复运行；当前是授权已完成，不是 B 实现或验收已完成。后文历史记录中的原 C5 目标以本次明确批准为准。编码角色应同步任务书及 AGENTS.md 的适用目标，复用已验证结果，不再重复请求同一语义批准。

2026-09-09（墨尔本）。本工单受《内部项目最小防护与工程纪律 v1.0》约束，不新增第4节所列防护；除第5节停止条件外，不因范围、沙箱、审计轮次、测试环境或文件清单请求人工批准。仅调整当前项目，未改 Co-op 平台源码、全局模型/推理配置或其他任务。

当前分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`；本轮改动仍在工作树中，既有 C5 未提交改动保留。

### 2026-09-09：B 最新修订历史数据研究模式（实施中）

管理者已批准 B：`latest_revision_research`（规则 `l1.10c5.market-evidence.v5`）是独立于 strict_snapshot 与 historical_reconstruction 的研究模式。它允许真实 Massive Basic 归档的后来取得历史值进入研究采用，但 public_at、source_version、revision_basis 仍为未知并持久化为 NULL；实际 fetched_at、原始对象、输入快照、run 与规则版本保留，报告标识“不保证当时可知”。这替代 C5 真实正例对历史版本见证的要求，不声称补齐旧模式的 PIT 证据。固定 AAPL 2024-11-02 真实归档已能解析为该模式的非 synthetic 候选；尚待一次性库的生产执行、恢复与断网重算验收。

本包定向非数据库测试 `.venv/bin/python scripts/test_project.py test --note 'B研究模式实现' …` exit 0：40 passed，证据 `var/test-runs/20260909T011043.849055Z-2`。一次性数据库入口于迁移连接前 exit 1（0 tests），`psycopg.OperationalError: connection is bad: no error details available`；证据 `var/test-runs/20260909T011053.667582Z-2/migrate.stderr.log`，未重复尝试。下一步由既有 executor 在已验证的隔离 PostgreSQL 环境运行固定 `bash scripts/run_l110c5_acceptance.sh`；它须使用新 v5 真实路径，完成迁移往返、真实 AAPL 研究采用、数据库 NULL 缺口保留、幂等、备份恢复及断网重算，首个非预期失败停止。

## 本轮变更

复用项目 Python 3.13 `.venv`，安装 pyproject 依赖与 pytest/ruff，版本固定在 `requirements-test.lock`。新增 `scripts/test_project.py`，同一工作包完成修复和测试，返回真实退出码，将命令、stdout/stderr、JUnit和机器统计保存在 `var/test-runs`，当前结果看 `latest.json`。

可丢弃 PostgreSQL `l110c5-project-test-9895d08f01351aac` 仅监听 `127.0.0.1:10004`，512MB/0.75CPU/PID128。普通 cargoq 可直接测试，未授予 Docker 权限。连接配置仅存 `.venv/test-runtime/database.json`，勿输出 DSN。生命周期只操作经项目标签验证的实例，等待正式 TCP 服务，并随容器清理匿名卷；单独实例的启动、连接就绪和删除卷已实测通过，编码用库继续保留。

固定容器验收包为 `scripts/run_l110c5_acceptance.sh`，直接复用 `scripts/l110c4_nuc_container_entry.sh`。排除本机 venv、var、缓存和真实环境文件，保留 `.env.example` 及必需归档。不再生成日期目录里的 commands.sh/container-wrapper.sh。

实际测试驱动修复：RunRoots 测试对象初始化；Golden fixture 的宿主固定路径依赖；迁移测试间 v4/release 数据残留；PostgreSQL 初始化临时进程被误认就绪；临时匿名卷遗留；恢复脚本误要求正式 summary 内嵌 manifest。生产报告本来就将 manifest 分文件保存，现按真实格式严格核对 manifest_identity、run、schema、cutoff、来源映射和证据，并拒绝虚构嵌入副本。所有投资与历史证据业务断言保留，没有 skip 失败测试。一次独立审查及同轮修复补核已完成，无剩余可操作问题。

## 固定入口与实际结果

在 `/srv/cargoq/projects/value-investing-system-l110c5` 下，以普通编码用户运行：

- 环境有变化时检查：`.venv/bin/python scripts/test_project.py env`。
- 同一修复包定向测试：`.venv/bin/python scripts/test_project.py test --note 本轮改动 tests/受影响测试.py`。
- 涉及数据库：`.venv/bin/python scripts/test_project.py test --database --note 本轮改动`；默认包含当前 C5 三个测试文件并先迁移。
- 工作包收尾一次完整回归：`.venv/bin/python scripts/test_project.py test --full`；无需等整个 C5 业务阶段结束，也不在每次小修改都重跑。
- NUC executor 的固定整包：`bash scripts/run_l110c5_acceptance.sh`；OUT 默认是新的唯一证据目录，首个非预期失败停止依赖步骤。生命周期 `db-start`/`db-stop` 由已有 Docker 权限的 executor 用同一 Python 入口执行。

首次开发验证80/80通过；全量首跑1207项中1203通过、4失败，0跳过，346.75秒。四个失败修复后相关41项全部通过，23.23秒。证据分别为 `var/test-runs/20260908T150422.429410Z-1173742`、`20260908T151148.362668Z-1185943`、`20260908T152050.426519Z-1208908`。

修复后完整回归：1207项全部通过、0失败、0错误、0跳过，366.68秒，exit0。证据 `var/test-runs/20260908T152142.850706Z-1210804/summary.json`，机器当前指针 `var/test-runs/latest.json`。新增入口ruff格式与静态检查、两个固定shell入口语法均通过。

Luna low executor 的最终固定整包退出0，证据 `/srv/cargoq/data/l110c5-validation-20260908T152138Z`。JUnit 79+1=80项全部通过、0跳过；六样本解析、一次性库迁移/持久化、精确三元组、重试幂等性、财务负例（预期exit2）、备份、独立恢复、恢复内容校验、断网新进程重算（exit0）、损坏对象副本拒绝（预期exit1）均已通过。前两次失败证据保留在 `...151207Z`（恢复启动竞态）和 `...151543Z`（summary格式），不将它们计为整包成功。

## 剩余事项与下一步

### 2026-09-09：s200 独立审查缺陷修复（编码阶段）

- `packages/connectors/market_evidence.py` 的 Basic v5 现按既有 `close_known_at` 日线完成语义选取不晚于 cutoff 的 bar，不把 Massive `t` 当作公开时间；SEC `disclosed_at` 必须不晚于 cutoff。固定早 cutoff `2024-10-31T23:59:59Z` 因 11 月 1 日 SEC 身份披露被拒绝；固定 `2024-11-02T00:00:00Z` 仍采用 11 月 1 日完成日线。必要 CIK/ticker/exchange/class/currency 匹配均须明确为 `True`；`primary_exchange=XXXX` 保持未知并拒绝，不能用 SEC NMS 覆盖。
- `apps/dashboard/exporter.py` 已接通 `l1.10c5.latest-revision-research-prepared.v1` 与对应 execution rule/hash，真实 s188 `v3_metric_report.json` 和 prepared 输入可由 `build_model` 配对；页面在该模式展示“后来取得的历史修订值；不保证当时可知”。跨模式配对和篡改输入 hash 继续拒绝。
- 财务缺口不再让已被 C5 采用的市场观察只停留在 result JSON：`LimitedSampleRunner` 在真实 C5 accepted、financial-gap 路径持久化 `market_observation`，但不补财务、不改变 financial failure/评分/投资结果。`scripts/assert_l110c5_archive_acceptance.py` 现查询对应行情行；v5 断言 `source_public_at/source_version/revision_basis` SQL NULL 与 `latest_revision_research`。这确认真实研究采用结果和行情行均可持久化，仍不表示 strict/historical reconstruction 的当时可知证据已经具备。
- 受影响非数据库命令 exit 0：`.../scripts/test_project.py test --note 's200 market cutoff identity dashboard fixes' tests/test_massive_basic.py tests/test_l111a_dashboard.py`；JUnit 81 passed、0 failures/errors/skipped，证据 `var/test-runs/20260909T022329.642687Z-2347436/summary.json`。真实一次性 PostgreSQL 命令 exit 0：`.../scripts/test_project.py test --database --note 's200 real archive market observation persistence' tests/test_l110c5_database_archive.py`；JUnit 2 passed、0 failures/errors/skipped，证据 `var/test-runs/20260909T022346.781087Z-2348209/summary.json`，environment 为 UID 1000、`net:[4026531840]`、数据库迁移成功。
- 收尾全量命令已按固定入口发起两次：`.../scripts/test_project.py test --full --note s200-review-fixes`。当前编码工具在约 30 秒后中断子进程，留下 `var/test-runs/20260909T022413.903186Z-2349130/` 的迁移 exit 0 与未完成 pytest 输出，未生成 summary/latest，故不宣称全量结果；未重复空跑。需要在可持续的同一已授权编码测试入口完成一次该命令并读取 summary。未改迁移，不重复 s196 往返；未启动 Docker 生命周期。
- 更正 s198 证据副本：实际 15 文件，且没有 `cleanup.command`；未补造历史文件。

2026-09-09 用户确认 Massive 已回复，并明确要求恢复 Co-op。已通过 Co-op 页面插入当前回函、1207项全量和80项整包通过结果，并确认任务 `996cf51a64` 的“管理决策74”恢复运行；原 roots 缺陷及固定验收包已经修复并验证，不再派发同一修复。继续使用当前流水线、固定测试入口和同一交接文件，完整历史按需读取。

工程拒绝链路已验证，不能将其充作 C5 业务完成。接下来继续固定 AAPL/KO cutoff 的真实历史采用正例：当前 Basic 存档缺 provider public time/version/revision 证据，仍应保持拒绝；需要有真实来源支持的补证，再做持久化与离线一致正例。旧 strict、PARA 负例、数据身份和来源检查保留。供应商回复已收到，详见下面的回函记录；此前等待 Basic 用途适用性回复的阻塞已解除。用户已授权本地真实归档、研究重放与可丢弃数据库测试，继续这些工作无需重复请示。不采购、不合 main、不部署。

## Massive 回函记录

用户于2026-09-09提供邮件截图，显示发件人“Cole from Massive.com”，正文回复：

> Thanks for reaching out,
>
> Yes, this is a suitable use case for our stocks basic plan.

用户确认该邮件是对我们此前研究用途询问的回复，并要求恢复 Co-op。项目记录据此更新为：Massive 已书面确认所述用途适合 Stocks Basic，继续当前个人研究、真实归档及重放工作，不再等待同一许可问题的答复。截图作为供应商回复证据保存在 [massive-basic-support-reply-20260909.png](evidence/massive-basic-support-reply-20260909.png)；截图本身未显示邮件日期或完整往来，不据此虚构逐项条款。供应商的用途回复不提供历史行情的 public time/version/revision 证据，该技术缺口仍需实际补证。

## 可核实的消耗变化

历史 C5 artifacts 在优化前后均为44个 commands.sh、19个 container-wrapper.sh；本轮三次执行复用同一个固定包，没有新增同类包装文件。交接只维护本文件，完整日志按需取。s146人工摘要的76通过与原始78通过/1失败不一致，现以JUnit统计为准。当前任务JSON没有token/usage字段，但本次进一步核对找到阶段日志的单次末尾 tokens used：m89=53,702、m90=45,590、m91=49,501、s178 executor=80,469；文件位于 Co-op data/996cf51a64/<阶段>.run1.log。m89至m91提示词约30.4–31.4KB，m92为29,447字节。字节数不等于token，也没有等工作量的前后对照，尚不能证明总token净下降或估报节省比例。此前环境和脚本优化已验证，但s176编码执行上下文仍出现数据库前置连接失败，s178转交executor后1233项通过；summary目录读取也有权限缺口。后续应具体修复项目结果可读性并复用已验证执行上下文，不应宣称所有往返已经消除。上述测试耗时仅是本次实测。初次旧清理方式产生的匿名卷未找到可核验的逐卷归属事件，未猜测删除或执行全局清理；修复后新实例清理已实测。

### 2026-09-09：s173 固定 8-K primary 实际执行（content_rejected，依赖停止）

本轮遵循《内部项目最小防护与工程纪律 v1.0》；保留历史失败原件，未改业务代码、测试、投资规则、平台配置、镜像或宿主权限。按管理者给定顺序执行；步骤2首个非预期失败后停止步骤3、步骤4及其依赖。

- 步骤1预检退出码 `0`。实际分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`，镜像 `l110c5-sec-backfill:dev` ID 为 `sha256:35beb5da44df63b1066b0a338a7d7f836cfedbdefb310f4674812871fbbf277a`。唯一新 OUT：`/srv/cargoq/data/l110c5-sec-primary-8k-4DpO3DgK`。四份归档输入可读，身份源仅作可挂载性检查，未读取或输出身份值。记录：`preflight.txt`。
- 步骤2使用显式 `--entrypoint /opt/l110c5-venv/bin/python` 及管理者指定挂载/资源限制；完整命令：`fetch.command.txt`。退出码 `1`，stdout/stderr：`fetch.stdout`、`fetch.stderr`，退出记录：`fetch.exit`。
- SEC 响应 HTTP `200`、请求数 `1`、固定最终 URL、body `40452` 字节与 `Content-Length` 一致；原件已保留。但机械校验为 `content_rejected`，原因：`DEI DocumentPeriodEndDate conflicts with actual submissions reportDate`。原件：`02-primary-8k.body`、`02-primary-8k.headers.json`、`02-primary-8k.metadata.json`。
- 步骤3、步骤4未执行；本轮未读取 `var/test-runs/latest.json`，不将旧统计写成本轮结果。步骤5 `git diff --check` 退出码 `0`。
- C5 仍未完成：未达到 `content_accepted`，未执行离线 verify，未形成固定 AAPL/KO 真实市场 accepted 正例。后续应处理字段冲突后仅对本 OUT 离线重验，不重拉；本轮不自动修复或重试。

### 2026-09-09：s174 DEI 原件离线证据交付

本轮遵循《内部项目最小防护与工程纪律 v1.0》；仅读取既有 SEC 原件和 submissions 归档，未联网、未读取 secret、未改业务代码/测试/宿主权限、未重拉或执行 verify/全量回归。唯一新证据目录：artifacts/l110c5/sec-backfill/owner-delivery-20260909-dei-evidence/。

- 已从 /srv/cargoq/data/l110c5-sec-primary-8k-4DpO3DgK 复制实际存在的 8 个指定文件；8 个文件逐文件 cmp exit 0。新副本为常规 0644，目录 0755；源目录未修改。sha256-comparison.txt 保留核对输出。
- 正文实际 DEI：DocumentPeriodEndDate 原文 October 31, 2024，format=ixt:date-monthname-day-year-en，contextRef=c-1；DocumentType=8-K、EntityCentralIndexKey=0000320193、EntityRegistrantName=Apple Inc.，均 contextRef=c-1。c-1 identifier 为 SEC CIK 0000320193，start/end 均为原文 2024-10-31。Date of Report 附近标签原文为 Date of Report (Date of earliest event reported)，前值原文为 October 31, 2024。
- submissions 原件第 147 行：accession 0000320193-24-000120、primaryDocument aapl-20241031.htm、form 8-K、reportDate 2024-10-31、acceptanceDateTime 2024-10-31T20:30:25.000Z。
- metadata 实际保留 HTTP 200、request_count 1、body 40452 与 expected 40452、remaining 0、目标/最终 URL；request_state=content_rejected 和原 content_error 未改写。完整机械 JSON：mechanical-extraction.json。
- 普通 cargoq 读取验证：本环境存在 uid/gid 1000:1000 的 cargoq；对新副本执行 su cargoq -s /bin/sh -c ...，目录列举、body 读取及 metadata JSON 解析均 exit 0。未切换源 0700 目录。
- 本轮首次组合命令的 submissions 子步骤因执行器路径拼写错误 exit 1；随后按实际存在路径独立重做并 exit 0。未执行编码修复、离线 verify、测试、重拉、Docker 或全量回归。C5 验收仍未完成；本交付只提供编码所需真实输入，不认定日期语义或放宽校验。

### 2026-09-09：s176 真实 8-K DEI 日期格式修复与离线复验

本轮遵循《内部项目最小防护与工程纪律 v1.0》；未重拉 SEC/Basic、未读取 secret、未修改身份、来源、PIT、strict 或 PARA 负例规则，也未改写 s176 原件或其 `mechanical-extraction.json`。

- 修复 `scripts/collect_l110c4_market_sources.py`：`DocumentPeriodEndDate` 仍保留原 `value`、attributes（含 `format`、`contextref`）和定位；新增独立 `normalized_value`。无 `format` 的既有 ISO 值仍由 `date.fromisoformat` 严格验证。仅支持已归档真实正文所用的 `ixt:date-monthname-day-year-en`，使用固定英文月份表与 `datetime.date`，不依赖系统 locale、不从 context/预期日期填值。未知 format、非法日期及规范值与 submissions `reportDate` 的真实冲突均明确拒绝。
- 回归补充真实 40,452-byte 原件（其不间断空白由既有 HTML 解析归一化）及 ISO 契约；另在测试临时副本覆盖真实日期冲突、非法日期和 unsupported format 拒绝。既有 viewer、短读/transport_failed、身份和 index/exhibit 关系测试保留。`.venv/bin/python scripts/test_project.py test --note '修复真实8-K DEI日期转换' tests/test_l110c4_market_evidence.py` exit `0`：JUnit 84 tests，84 passed，0 failures/errors/skipped；证据 `var/test-runs/20260908T235329.184783Z-2/summary.json`。
- 离线 CLI 复验 exit `0`，无网络和 secret：新目录 `artifacts/l110c5/sec-backfill/verify-20260909-dei-date-normalization/` 只含本次所需 primary/index/submissions/exhibit 副本及 `verify.*`；`mechanical-extraction.json` 记录 `content_accepted`。复验结果保留 primary 原始 `October 31, 2024`、`ixt:date-monthname-day-year-en`、`c-1` 与规范 `2024-10-31`，并与 submissions `reportDate=2024-10-31` 比较通过。复制的原 metadata 仍保留 `request_state=content_rejected` 及原 `content_error`，没有就地覆盖 s176 原件。
- 收尾全量命令 `.venv/bin/python scripts/test_project.py test --full` exit `1`，在迁移前置而非 pytest 失败：现有一次性 PostgreSQL 返回 `psycopg.OperationalError: connection is bad: no error details available`。机器统计为 0 tests/0 passed/0 failures/0 errors/0 skipped；证据和当前 `var/test-runs/latest.json` 均指向 `var/test-runs/20260908T235400.059742Z-2/summary.json`，stderr 为同目录 `migrate.stderr.log`。未重复空跑、未改宿主权限；待既有 executor 恢复其已验证的一次性数据库后，可从这一条全量命令继续。
- `git diff --check` exit `0`。SEC 本文 `content_accepted` 仅补齐 SEC 来源材料，不能等同固定 AAPL/KO 的真实市场采用、持久化或离线一致正例；市场 public time/version/revision 的既有字段缺口仍未被 SEC acceptance 填补，C5 未完成。

### 2026-09-09：s178 DEI 日期转换修复包收尾全量回归

本轮遵循《内部项目最小防护与工程纪律 v1.0》；未改代码或测试，保留当前工作树及全部历史证据。实际分支 `codex/l110c5-real-market-data`，HEAD `02b6e23866bfdeb111e118c5b47112421927ea36`，运行身份为 `root`。本轮唯一执行证据目录：`var/test-runs/20260909T235637.534987309-s178-full/`。

- 步骤1预检和证据目录创建 exit `0`；命令、stdout/stderr、退出码保存在该目录的 `preflight.txt`、`db-start.*`、`full.*` 和 `diff-check.*`。未生成 commands.sh 或容器 wrapper。
- 步骤2 `.venv/bin/python scripts/test_project.py db-start` exit `0`，复用本项目一次性容器 `l110c5-project-test-9895d08f01351aac`；未执行恢复分支。未输出 DSN、完整 inspect 或环境变量。
- 步骤3全量回归 exit `0`；`var/test-runs/latest.json` 指向 `var/test-runs/20260908T235645.759331Z-2084200/summary.json`。机器统计 1233 tests、1233 passed、0 failures、0 errors、0 skipped，耗时 368.76 秒。
- 步骤4 `git diff --check` exit `0`，stdout/stderr 均为空。
- 本轮未执行定向84项、SEC离线verify、容器备份恢复整包、单独迁移往返或独立审查。全量通过仅完成 s178 修复包验证，不认定独立审查或 C5 最终通过。固定 AAPL/KO 真实市场采用、持久化、离线一致正例及 public time/version/revision 证据仍未完成；本包未读行情密钥、未联网补采、未采购、未发邮件、未合 main、未部署。
