# L1.10C-4 阶段成果与新流水线续接

日期：2026-09-08。目标：继承原工单成果，完成剩余修复、定向验证和任务分支交付。

## 续接位置

- 原 Co-op 任务：`063c5fec98`，标题「L1.10C-4 历史市场身份与价格证据接线」。交接时页面仍显示运行中，已中止以避免并行改写；历史保留。
- 唯一工作区：`/srv/cargoq/projects/value-investing-system-l110c4`。
- 分支：`codex/l110c4-market-evidence`；基线 HEAD：`57275231a64af8900de17612279aba873e11bf6c`。
- C-4 修改仍未提交。直接继承现有修改与未跟踪文件，禁止 reset、clean 或从基线重新实现。
- 原范围：`docs/design/L1.10C4_work_order.md`；工程纪律：`docs/开发任务书/00B_内部项目最小防护与工程纪律-v1.0.md`。

## 已有成果及证据边界

1. 已实现历史市场来源归档、确定性解析、财务与市场组合输入、prepare/execute/replay/recover、来源关联与看板消费。真实观察、工程合成正例和实际采用分开记录。
2. 唯一全量测试原始记录为 **1041 passed / 22 failed**；22 个相同 nodeid 已通过互不重叠的 **13 + 7 + 2** 定向复测闭合。已有测试员核对和 `0011→0010→0011` 迁移成功记录。不能改写为一次全绿，也无需因新建任务重跑全量。
3. 已完成一次独立审查，发现 R1–R7，原结论为「暂不通过」。新任务继承这次审查；逐项修复与定向验证后由管理者核对闭合，不重新完整审查。
4. R1/R5：六个 AAPL/PARA/KO 正常及篡改多公司页面通过 DOM 核验，市场状态来自匹配执行身份的持久化 summary。
5. R2/R3：真实 PARA gzip 已能提取 2024-11-15 Close/Adj Close 10.84、Volume 14,795,800。最新真实 PARA-v3 链覆盖 execute/repeat/replay/recover、严格来源比较和 SQL；12 项定向测试及 1 项 429 测试通过。晚于 cutoff 的捕获仍拒绝采用。
6. R6/R7：SEC 行/context 隔离与冲突检测、拆股分子/分母解析已有定向通过证据。

主要索引（均在唯一工作区）：

- `artifacts/l110c4/final-delivery-preparation/R1-R7-evidence.json`：现有六项支持闭合，R4 待验证；部分审查原文缺失描述已过时，需更新。
- `artifacts/l110c4/final-acceptance/tester-reconciliation/`：原始 22 项失败的 nodeid 对账。
- `artifacts/l110c4/review-fixes/execution/final-three-groups/six-case-pages/browser-dom.json`：六页浏览器证据。
- `artifacts/l110c4/review-fixes/execution/real-para-source-contract-retest/openclaw-20260908-1/INDEX.md`：最新真实 PARA-v3 证据。
- `docs/runbooks/L1.10C4_delivery.md`：交付范围和现有证据导航。
- `docs/runbooks/L1.10C4_review_original_20260908.md`：从旧任务 s156 的实际 output 原样导出；保存历史「暂不通过」结论，不冒充复审。

## 当前剩余问题

R4 是主要剩余工程门禁。既有组合样本在取得非空 SEC submissions 后因 unsupported SIC 拒绝，要求 C4 保留该财务来源与身份路由；只删除隔离副本内的该 SEC 对象时，prepare 和非缓存 Runner 必须明确失败，且不能产生成功结果或成功缓存。

最新已完成执行：`artifacts/l110c4/review-fixes/execution/r4-runner-coverage-retest/INDEX.md`。外层退出 1；环境、依赖、迁移通过；缺源 prepare 和 Runner 均退出 2。执行摘要将 failed 记录描述为触发成功判据，仍需按实际 SQL 与状态合同核对，不能凭摘要判定生产缺陷。

原任务 m104 已要求修正这一判据。s208「修正失败判据」在交接中止前已有未提交修改，但没有最终执行者输出：verifier 拆分 run 与真实公司结果、允许合法 failed 状态，新增区分成功与失败的判据测试并接入固定容器入口。最后可见本地 unittest 因 `psycopg` 缺失而导入失败，不能声称已验证；应沿用已有 NUC 镜像和一次性 PostgreSQL 验证。

最后三个阶段的精确输出另存 `artifacts/l110c4/continuation-20260908/`。优先阅读 `m104-output.md` 与最新 R4 INDEX；不要再次加载旧任务全部派发。

## 新任务执行要求

1. 先核对现有 s208 变更是否完整，完成 R4 判据修复及固定续测交接。按现有合同区分合法失败记录与成功状态、成功产物、缓存复用，核对全部匹配输入身份的 run/公司结果/来源链接。保留 prepared 字节和身份；只删除隔离副本内目标 SEC 对象。
2. 沿用已验证容器与临时数据库入口，由命令执行角色运行必要的 R4 定向测试。新建证据目录，保存真实命令、stdout/stderr、退出码、缺源诊断、SQL 前后态和正常链证据。失败直接定位修复；不要反复运行未变化的失败命令或扩展无关验证。
3. 继承 R1/R2/R3/R5/R6/R7 的有效证据及原审查。核对剩余问题后更新 R1–R7 闭合表、测试结论和交付文档，明确实际验证范围。原审查结论与后续管理闭合结论分别注明。
4. 工程门禁闭合后，将授权源码、测试、任务书、文档与重放所必需的最小可读材料提交到现有任务分支；非强制推送至已配置 origin，记录 FULL_SHA，并从 `/srv/git/value-investing-system.git` 回读验证。大型中间产物留作本地证据，不机械全量加入 Git。
5. 完成后给出最终代码交付状态、提交与证据路径、仍保留的数据缺口。范围内修复、必要依赖、一次性数据库、文档和任务分支提交推送已授权，依工程纪律自主完成。

## 数据与范围边界

四个真实样本仍全部拒绝采用。AAPL/KO 的三份 Yahoo 响应为 429；真实目标 SEC 原文未请求/取得；PARA 捕获晚于 cutoff。历史证券类别/交易所及 ticker 连续性、提供方绑定、币种、完整行动及调整基础、闭合真实财务输入仍不足。这些必须如实列为数据余缺，不能用 synthetic 正例填补或放宽 PIT 制造采用结果。

冻结原工单样本、cutoff、投资公式和 paper-only；不改 KO 映射，不将 EPS 加权股数替代时点股数，不做 L2/L3、全市场扩展、付费采购、main 合并或部署。

## 简短上下文运行方式

采用平台现有 Astra 管理、实施、测试、审查、文档和命令执行角色配置。每次只交付当前目标、未解决问题、最新结果及证据路径；早期历史按需搜索。不要复制全部历史派发、额外调用模型总结或因换任务重置测试/审查次数。平台报告的 96.24% 是提示词字节体积降幅，实际额度节约尚未验证。
