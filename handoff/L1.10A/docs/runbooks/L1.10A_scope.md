# L1.10A：真实字段缺口与财务语义核查

日期：2026-09-06。基线：`ee8c8953a8538878dfef9a0944730cd669950f92`。

本工单受《内部项目最小防护与工程纪律 v1.0》约束。不得新增其第4节所列多余防护；除第5节真实停止条件外，不得因范围、沙箱、审计轮次、测试环境、文件清单或普通工程失败请求人工批准。

本包输出诊断与下一包实施清单，不重新验收 L1.9，不部署或改写旧结果，不更改投资规则。

## 冻结诊断样本

复用 `config/l18_limited_real_sample.v1.json` 的六家公司、证券身份和 `2024-11-18T00:00:00Z` cutoff：KO（消费）、CAT（周期）、SNOW（高 SBC）、PARA（高杠杆/身份恢复）、TSM（FPI/ADR）、JPM（行业不支持负例）。选择依据为既有覆盖形状与归档可复用，不按候选或评分结果筛选。本包不新增诊断公司，不把这六家视为统计代表样本或正式候选。

输入来自 NUC 既有 `value_investing_l1_staging_limited_sample_cache` 中的 12 个 SEC response body；仅在本机离线副本读取。报告保留源 body hash、CIK、accession、观察期间与定位。网络核验仅补官方语义说明，不替换原归档。

## 要回答的问题

1. 股数、现金、CFO、CapEx、利润为何被选择或丢弃？源缺失、frame/期间筛选、taxonomy/币种映射分别造成多少缺口？
2. 期间加权平均股数是否流入时点股本、fully-diluted denominator 或市值字段？
3. 债务、TTM、历史观察、行业分位与门槛证据是否有批量生产者？
4. 哪些数据必须外购，哪些先修公开数据解析/接线，哪些继续 unknown？

## 退出条件

- 完成实际归档与基线解析器的逐字段对照，记录可复算案例和不可据此推断的结论。
- 形成 L1.10C 实施顺序及需要用户裁决的具体口径；不为证明候选非零而放宽输入。
- 诊断离线可重放，不访问数据库、不发布候选。若只交付诊断文档与诊断工具，验证其实际输入输出及确定性，不重跑无变化的 L1.9 全套验收。
