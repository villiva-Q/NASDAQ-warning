# L1.10C-3 configurable v3 最终交付交接

本工单受《内部项目最小防护与工程纪律 v1.0》约束：未新增其第 4 节所列防护；保持既有投资口径、数据许可、PIT 和 paper-only 边界。本页只整理已验证事实，不替代或改写原始审查结论。

## 当前状态

- 唯一目录 `/srv/cargoq/projects/value-investing-system-l110c3`；未发现适用 `AGENTS.md`。分支 `codex/l110c3-configurable-v3-sample`。
- C-3 实现、配置、测试和交付文档已由实现提交 `8b72d0477387b758bd3768fd5981576a13ca070b` 收口，并已非强制推送到 NUC 裸仓库目标分支；测试及 reviewer R1–R3 修复验证均已完成。
- 唯一 reviewer 原始结论仍是“不通过”，原文保留在 `docs/runbooks/L1.10C3_review.md`。R1 请求失败来源串用、R2 旧四季度遮蔽更新 FY、R3 当前身份误作历史身份均已修复并由定向证据闭合；这不是 reviewer 再次审查或重新签署通过。
- 本页回执更新后由独立文档提交收口；不合 main、不部署。

## 正式入口与配置合同

正式入口是 `scripts/run_l110c3_configurable_sec_history_v3.py`，不是测试包装。配置版本是 `l1.10c3.configurable-real-sample.v1`，包含 timezone-aware `point_in_time_cutoff`、CIK/ticker/行业路由及 market identity（exchange/currency/security_type/issuer_terms）。拟提交配置 `config/l110c3_configurable_sample.v1.json` 是旧六家公司中的 KO 子集加新增受支持的 AAPL；AAPL 为 NMS/USD/EQUITY，KO 为 NYQ/USD/EQUITY。

配置是闭合输入：CIK/ticker/发行人/交易所/币种/证券类型会与 SEC 和市场响应核验；重复或冲突拒绝。配置不能扩大行业支持：金融、IFRS/ADR/FPI 仍不支持。cutoff 必须可解析、有时区、非未来，UTC 值进入 config/input/run/snapshot/report identity。SEC 原始 bytes 可按来源身份复用，但真实 `fetched_at` 不改写成 cutoff，过滤结果和 run 不跨 config/cutoff 串用。

仅在既有 NUC Python/依赖、SEC User-Agent 与隔离 PostgreSQL 环境执行；DSN 只经进程变量 `TEST_DATABASE_URL`（可用 `--database-url-env` 改名）注入，不写入命令或日志。`DATABASE_URL` 仅属既有迁移流程，本包无新迁移。使用全新的绝对 workdir/output；output 独占创建。`--company-budget` 是 prepare/recover 正整数上限，示例为 2。

```sh
python scripts/run_l110c3_configurable_sec_history_v3.py prepare \
  --config /workspace/config/l110c3_configurable_sample.v1.json \
  --workdir /workspace/artifacts/l110c3/operator-new \
  --output /workspace/artifacts/l110c3/operator-new/prepare.json --company-budget 2
python scripts/run_l110c3_configurable_sec_history_v3.py run \
  --config /workspace/config/l110c3_configurable_sample.v1.json \
  --workdir /workspace/artifacts/l110c3/operator-new \
  --output /workspace/artifacts/l110c3/operator-new/run.json
```

`replay`/`recover` 只接收闭合 input；不带外部 config/cutoff 时继承其中 `sample_config` 的实际配置、cutoff 和 identity。给出不一致 config/cutoff 会在连接数据库前拒绝，不能重解释旧快照。

```sh
INPUT=/workspace/artifacts/l110c3/operator-new/input/configurable_sample_financial_period_v3.json
python scripts/run_l110c3_configurable_sec_history_v3.py replay \
  --input "$INPUT" --workdir /workspace/artifacts/l110c3/operator-replay-new \
  --output /workspace/artifacts/l110c3/operator-replay-new/replay.json
python scripts/run_l110c3_configurable_sec_history_v3.py recover \
  --input "$INPUT" --workdir /workspace/artifacts/l110c3/operator-recover-new \
  --output /workspace/artifacts/l110c3/operator-recover-new/recover.json --company-budget 2
# Expected rejection before database use:
python scripts/run_l110c3_configurable_sec_history_v3.py replay --input "$INPUT" \
  --cutoff 2024-11-02T00:00:00Z --workdir /workspace/artifacts/l110c3/operator-mismatch-new \
  --output /workspace/artifacts/l110c3/operator-mismatch-new/replay.json
```

旧 C-2 固定入口和 L1.8 默认行为兼容；旧 prepared 可读。旧 v2/更早 run 保留，不因 v3 rule 被重解释或覆盖。prepared v3 和执行规则为 `l1.10c3.configurable-sec-history-prepared.v3` / `l1.3.v1+c3-completed-fy-ttm.v2`。

## 真实样本与 PIT 对照

AAPL 两个 prepared v3 输入均来自官方 SEC，跨越 2024 10-K `0000320193-24-000123` acceptance `2024-11-01T10:01:36Z`。以下是最新 v3/rule-v2 正式记录；旧 rule/v2 run 仅为历史记录。

| cutoff | config / input / run | snapshot | 五项 USD 与组成 |
| --- | --- | --- | --- |
| `2024-10-31T23:59:59Z` | `28001555…65515` / `15487cbc…fb66` / `8f259f7b-66cc-4b2f-a062-d41cfee5f621` | `c48e6ffd…ff3745` | capex 8,702,000,000；CFO 113,041,000,000；EBIT 120,594,000,000；net income 101,956,000,000；SBC 11,455,000,000。每项为 2023 FY `0000320193-23-000106` + 2024 Q3 YTD − prior Q3 YTD（后两项 `0000320193-24-000081`）；acceptance 为 `2023-11-02T22:08:27Z`、`2024-08-01T22:03:34Z`，均不晚于 cutoff。 |
| `2024-11-02T00:00:00Z` | `5aad1384…79f0` / `56987eb0…d55b` / `33adcc6f-be04-460a-ba13-a19bc561d56a` | `379eaed5…598c6` | capex 9,447,000,000；CFO 118,254,000,000；EBIT 123,216,000,000；net income 93,736,000,000；SBC 11,688,000,000。每项是该 10-K、`2023-10-01..2024-09-28` 的完整 FY 单组成。 |

报告、snapshot、来源及命令 stdout/stderr/exit 索引为 `artifacts/l110c3/review-fixes-nuc/` 和 `docs/runbooks/L1.10C3_review_fixes_nuc_validation.md`。真实选中组成均为 `recent`；history 已登记但未称为真实选中组成。跨 history、请求故障和旧四季度/完整 FY/FY 后 partial 的候选竞争均为合成用例。

tester 在比较报告前以官方 companyfacts/submissions 独立复算：`110543000000 + 91443000000 - 88945000000 = 113041000000 USD`；after FY 原始值是 `118254000000 USD`。这证明是 acceptance 改变造成 PIT 选择变化，而非只改输出日期。同一 after v3 闭合输入重复 run 返回同一 `33adcc6f-…`/input、`replayed=true`，object/archive/run/screen/result/filing/fact/evidence/market 九类计数不增长；无 config 的 replay/recover 继承成功，错误 cutoff 明确拒绝。

主配置实际 route provenance run 是 `e4c4fbfc-b1b5-4473-b882-63396368cd15`；此前 completed-FY 最终 rule run 是 `0dd4f05c-3bba-47c9-9093-cd7caf009b6b`。AAPL 可用上述 SEC 财务。KO excluded，五项均 unsupported：官方 CIK `0000021344`、SIC `2080`（Beverages）、SHA `96cbd37c…bb175`、真实 fetched_at `2026-09-06T13:04:01.114852Z`、reason `industry_route_unsupported_unmapped_sic`。新拒绝记录回读 URL/SHA/fetched_at/route/reason；旧失败保持可读但不伪造缺失来源。市场 fixture 只证明接线；无 cutoff 身份证据的市场输入为 missing/unsupported，未声称真实历史行情、候选或排名通过。

## 验收与证据

| 项目 | 已保存事实 / 索引 |
| --- | --- |
| 原包级与环境补测 | 原包级历史 `855 passed, 1 failed, 116 errors`，未伪称新全量；116 DB node 后续 `116 passed`、原 `/etc` node 后续 `1 passed`。`docs/runbooks/L1.10C3_tester_nuc_followup.md`，`artifacts/l110c3/tester-nuc-followup/`。 |
| C-3 定向 PostgreSQL | reviewer fixes `30 passed, 17 deselected` exit 0；completed-FY `7 passed`；来源留存 DB node `1 passed`、直接三例 `3 passed`。见 `L1.10C3_review_fixes_nuc_validation.md`、`L1.10C3_completed_fy_nuc_validation.md`、`L1.10C3_route_provenance_nuc_validation.md`。 |
| R1–R3 修复 | R1 两种 prepare→recover 请求失败顺序、R2 完整 FY、R3 SEC exchange 冲突持久化通过；原五节点为 4 passed/1 failed，保留在 `artifacts/l110c3/review-production-path-nuc/`。 |
| R2 最终闭合 | 原失败是测试误将 Q1 `quarterly` 期望为 `ytd`，历史日志保留。核正后 `tests/test_l110c3_configurable.py::test_c3_cli_synthetic_sec_periods_stage_and_adapter_choose_latest_legal_ttm[True-False-expected1-accessions1]` 在 `vis_l17_test_db_l110c3test000001` 为 `1 passed in 0.32s`、exit 0；FY 150/annual、prior Q1 10/quarterly、current Q1 50/quarterly，`150 + 50 - 10 = 190`，并通过 snapshot/PIT/source-SHA 回读。`artifacts/l110c3/review-r2-period-kind-nuc/INDEX.md`。 |
| 审查分列 | 原不通过：`L1.10C3_review.md`；修复闭合：`L1.10C3_review_fixes.md` 与 `L1.10C3_review_fixes_nuc_validation.md`。 |

无新 migration，未机械迁移往返。本次只做文档事实、路径和 CLI 合同核对，没有重跑测试、全量回归或独立审查。

## OpenClaw 提交与推送回执

唯一目标分支为 `codex/l110c3-configurable-v3-sample`，origin 为 NUC `/srv/git/value-investing-system.git`，基线为 `b4b838bd0db8385e83217b90fa9acaec0916ca5e`。实现提交 A 包含三个修改生产文件、C-3 配置、正式 CLI、测试及 C-3 runbooks（含本页的提交前版本），共 16 个文件；ignored 原始归档、数据库/运行日志、缓存及凭据均未纳入。

- 实现提交 A：`8b72d0477387b758bd3768fd5981576a13ca070b`，摘要 `feat: add configurable L1.10C3 v3 sample entrypoint`。
- 实际执行时间：`2026-09-07T00:39:39+10:00`（回执采集时间）。
- 第一次 push：普通 `git push origin codex/l110c3-configurable-v3-sample:refs/heads/codex/l110c3-configurable-v3-sample`，exit 0；创建目标分支，无 force。
- 裸仓库回读 A：exit 0，`8b72d0477387b758bd3768fd5981576a13ca070b`，与本地 A 一致。
- 第一阶段证据：`artifacts/l110c3/final-push/push-a.*`、`bare-readback-a.*`；最终文档提交 B 和最终裸仓库回读记录在同目录索引与管理者返回消息中，避免为把文档自身 SHA 写入自身而循环提交。

不得自动合 main、部署、修改宿主权限或进行真实交易。
