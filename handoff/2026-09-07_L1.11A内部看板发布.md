# L1.11A 内部看板发布

- 入口：[价值研究工作台](https://villivanuc.tailee114c.ts.net/value-research)。
- 用户明确批准该批研究结果、该地址和仅其Tailscale网络内获准设备的访问范围。
- main及源码版本：`57275231a64af8900de17612279aba873e11bf6c`。
- 范围：离线静态研究快照，四个实际结果集；没有实时数据库读取、自动抓取、评分、正式排名或交易动作。L2/L3仍未启动规划。
- 方式：现有Tailscale Serve HTTPS443的 `/value-research` 指向一个确定的 `index.html` 文件。原主页代理 `http://127.0.0.1:8790` 与8443 Co-op代理 `http://127.0.0.1:5211` 均保持不变；未启用Funnel，未新增常驻HTTP进程、端口或systemd服务。
- 页面位置：`/srv/cargoq/projects/value-investing-system-l111a/artifacts/l111a/internal-preview-5727523/site/index.html`。需要保留该发布文件，不能清理该目录后仍期望入口可用。
- 数据：从该checkout的 `artifacts/l111a/real-inputs/` 导出，公开导出exit 0；四个结果集，AAPL before/after CFO为113041000000/118254000000 USD；C-2六公司18/30。缺失/unsupported和历史身份限制如实呈现。
- 访问验证：本会话浏览器直接打开HTTPS地址，实际DOM显示上述before结果；切换aapl-after后显示118254000000 USD、FY组成、2024-11-02 cutoff和对应run_id。无旧验收重跑。
- NUC回执目录：`artifacts/l111a/internal-preview-5727523/` 内 `release.json`、`serve-before.json`、`serve-after.json`、`export.*`、`publish.*`。发布exit 0。
- 页面SHA256：`d6c67286419e59fcacde92cb5a2ba9cdc2bb29337df12c30a4bdc92e3938100e`。

需要撤下时只移除本路径：`tailscale serve --https=443 --set-path=/value-research off`。不要执行全局 `serve reset` 或443整体off，以免影响既有主页。

这是单一所有者内部预览发布，不是完整应用staging/production部署。后续更新按真实报告重新导出并验证具体发布文件，保留现有来源、缺失语义和paper-only边界。
