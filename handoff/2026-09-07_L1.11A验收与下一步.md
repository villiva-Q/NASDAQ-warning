# L1.11A 验收与下一步

> 发布完成更新：用户已明确批准将这批研究结果发布到 `https://villivanuc.tailee114c.ts.net/value-research`，仅供其 Tailscale 网络内获准设备访问。现已通过 Tailscale Serve 在既有443下增加该单文件路由，命令exit 0；既有 `/` 代理与8443 Co-op配置保持一致，未启用Funnel。浏览器从该HTTPS地址成功打开，并确认AAPL before CFO113041000000 USD与after CFO118254000000 USD及对应组成/run/cutoff。已保留看板标签供用户查看。发布回执位于NUC `artifacts/l111a/internal-preview-5727523/release.json`，含源码SHA、页面hash、原始输入路径、导出/发布时间与撤下该路径的固定命令。此前自动审核阻断已经用户具体授权解决；下方等待批准文字为历史。当前是静态研究结果内部预览，非实时数据库服务，不等于完整L1 staging/production应用部署。

> 后续执行更新：用户要求“推进执行”后，main 已以条件快进集成到 `57275231a64af8900de17612279aba873e11bf6c`。真实归档已重新导出到 NUC `artifacts/l111a/internal-preview-5727523/site/`，exit 0，四个结果集可用；本地可审阅页面为 [index.html](./L1.11A/preview/index.html)。没有重跑采集、评分或旧测试。拟通过现有 Tailscale Serve 443 的 `/value-research` 路径仅提供单一静态页面，保留既有主页及8443 Co-op入口、不启用Funnel或新增服务。该发布操作被自动审批审核拒绝：认为“推进执行/内部预览”未明确授权真实内部研究结果向这个tailnet地址发布。尚未修改Tailscale路由，需用户明确确认此地址与tailnet访问范围；不通过Co-op或其他通道绕过拒绝。

## 当前结论

用户转交 Co-op `782f603ef2` 管理决策31完成通知后，本会话只读核对交付文档、独立测试闭合索引、原始reviewer报告、修复记录、最新浏览器原始stdout/退出码、最终推送回执及Git refs。未重跑测试/浏览器/审查，未合main或部署。

- 最终FULL_SHA：`57275231a64af8900de17612279aba873e11bf6c`。
- 已验证实现SHA：`7edf6e5913678bdecd7e414cd49df35c9208ab2c`；独立测试和原始reviewer检查点为 `d3196cfcd4962921a7cb74a5d91553f737ec1479`。
- 最终任务分支：`codex/l111a-l1-dashboard`，NUC裸仓库 `/srv/git/value-investing-system.git` 回读一致；checkout `/srv/cargoq/projects/value-investing-system-l111a` 工作树干净。
- main 仍为 `132c9b5048ce46ffecfca394f050a62e012b7400`，merge-base与main一致，可以fast-forward。
- 从已验证实现到最终SHA只有两份runbook的文档变更，共195行新增；无后续产品代码修改。

本包可按三层只读dashboard、第一层实际报告接线的范围收口。它不是实时数据库看板或完整市场筛选验收，不代表L2/L3业务已实现。

## 已验证成果

Python从实际报告、prepared snapshot、run及关联SEC材料生成JSON视图模型和离线页面；更新输入后重新导出即可刷新，不修改前端，不触发采集、评分、数据库写入或候选发布。

AAPL两个独立run/cutoff分别呈现CFO 113041000000与118254000000 USD；前者FY110543000000 + current91443000000 − prior88945000000，后者新完整FY。期间、taxonomy、accession、acceptance、币种依据、来源及运行身份已进入页面。C-3主路由5/10与KO SIC2080 unsupported、C-2六家公司18/30与KO CFO5524000000保持分开；L1.9的300家试跑单列。L2/L3是明确未启动的规划信息架构。

最新 `artifacts/l111a/nuc-unit-assertion-retest/browser-command.exit` 与 `browser/cdp.exit` 均为0；原始 `browser/cdp.stdout` 尾行为 `{"passed":59,"total":59,"all":true}`。该次索引记录14/14场景、运行时异常0及中文桌面/360px截图。本会话读取了原始结果，未重新执行或亲自目视这些截图。

独立tester已核对唯一历史全量 `886 passed, 1 failed, 122 errors` 的未通过集合：122个数据库node ID与补测列表差集0，一次性PostgreSQL中122 passed；另1项为宿主配置继承导致的权限错误，在隔离环境保留原断言1 passed。不称为另一次全量全绿。

唯一原始reviewer结论为四项P2不通过，原报告保留。R1嵌套运行/manifest身份冲突、R2必要来源/期间/运行信息未显示、R3损坏结构误作正常缺失或0公司、R4缺失L2/L3规划分区，已由实现者修复并以27项定向测试及上述浏览器验证闭合；没有第二次reviewer签署。R2原58/59的连续文本断言失败也保留，后续改为同组成容器中关联金额、币种、角色、accession和taxonomy并有正反例验证。

## 交付与复现材料

权威交付：NUC checkout `docs/runbooks/L1.11A_delivery.md`；最终回执 `artifacts/l111a/final-handoff/FINAL_RECEIPT.md`；独立测试闭合 `artifacts/l111a/independent-acceptance-closure/INDEX.md`；原审查 `docs/runbooks/L1.11A_review.md`；修复及最新NUC索引见交付文档。

公开固定入口：

```sh
cd /srv/cargoq/projects/value-investing-system-l111a
bash scripts/export_l111a_c3_inputs.sh artifacts/l111a/delivery-preview
python3 -m http.server --bind 127.0.0.1 --directory artifacts/l111a/delivery-preview 8000
```

这是复现说明，本会话未启动该命令。源码checkout不包含ignored真实输入和截图：重做导出需 `artifacts/l111a/real-inputs/` 下aapl-before、aapl-after、ko-route、c2-six-company、c3-common及关联SEC对象。来源、对象hash和原读取位置见 `artifacts/l111a/environment-handoff/INPUT_INDEX.md`。集成和内部预览时交接这批机器材料，不靠设计草图常量或旧视图模型反向生成权威输入。浏览器证据重做还需已有字体/Chromium环境，但无新变更不必为集成重跑。

## 下一步

1. 用户授权推进集成时，在NUC干净上下文条件快进main至最终5727523，不在Mac脏工作树操作，不重复旧验收。
2. 同时交接真实输入和已生成静态页面，为用户提供可访问的内部预览。若需NUC staging部署或改现有服务路由，先形成具体部署方案，按既有里程碑授权边界执行；本完成通知不等于部署授权。
3. 页面可用后继续L1真实数据闭合：历史证券身份/价格与企业行动、KO实际行业映射、股本/估值分母/现金债务，再做真实小样本完整筛选链。dashboard完成不消除上述缺口。

遵循《内部项目最小防护与工程纪律v1.0》，保持投资口径、许可、paper-only；无新缺陷不补第二轮审查或证明链。
