---
title: L1.10C-1 验收与下一步
date: 2026-09-06
status: active-handoff
---

# 当前结论

> 执行更新：用户随后授权“推进下一步执行”。NUC main 已从 ee8c895 原子 fast-forward 到 `4ec977cc80871dfff23ad6a0231a25be2b720b0f` 并回读一致。C-2 已建独立 checkout 并通过 Co-op 启动，当前进展见 [C-2 执行记录](./2026-09-06_L1.10C-2_Co-op执行记录.md)。下列“尚未集成”和工程顺序保留为本次授权之前的核验记录。

L1.10C-1 在约定的显式 v3 财务期间解析、多申报 TTM 接线及输入快照范围内可以收口。此次只做交付证据核验，没有新一轮独立审查、测试或部署。原审查“不通过”作为历史结论保留；四项缺陷由修复记录和针对性验证分别闭合。

- NUC 任务分支 `codex/l110c1-financial-period-inputs`：`4ec977cc80871dfff23ad6a0231a25be2b720b0f`，已回读一致。
- NUC main：`ee8c8953a8538878dfef9a0944730cd669950f92`，尚未集成本包。
- 已确认 main 是任务分支祖先，可 fast-forward；任务 checkout 以所有者 cargoq 执行 Git status，工作树干净。
- 该分支包含 L1.10A 诊断提交，后续直接集成最终 SHA 即可，不需要分别重复合并诊断分支。
- 权威仓库 `/srv/git/value-investing-system.git`；独立 checkout `/srv/cargoq/projects/value-investing-system-l110c1`。
- [最终交付摘要副本](./L1.10C1/docs/runbooks/L1.10C1_delivery.md)，来自上述最终提交的 `docs/runbooks/L1.10C1_delivery.md`。

# 核验结果与准确边界

交付增加默认关闭的 `stage_company_financial_period_v3(...)`，保留 quarter/YTD/annual、每条申报 acceptance 与来源；支持调用方显式提供历史 submissions 页；实际 duration facts 进入 adapter，并形成可回读的独立内容寻址输入快照。默认旧入口继续使用 v2。

修复前真实 PostgreSQL 环境完整回归 934 项通过；修复后 72 项受影响测试通过。不得称为最终提交全量 934 项通过。原独立审查报告是管理者保存的结论摘要，不是 reviewer 原文。R1 比较 YTD、R2 输入快照、R3 同值多来源与异值冲突、R4 legacy 期间阈值的修复及具体测试节点见交付摘要和 NUC implementation report。

已读取修复后 PostgreSQL 原始结果与日志，并核对最终提交验收脚本：KO CFO TTM 为 `11,599,000,000 + 2,854,000,000 - 8,929,000,000 = 5,524,000,000`；清空 raw_facts 后结果缺失。快照回读和旧 v2 不变断言位于结果写入与 acceptance 输出之前。脚本 SHA256 与执行记录一致，为 `6c4591142571d981db4f6234af3896fe16f83528fe2eae5b0d4886ad5264fe47`。

外层 zsh 因只读变量 status 赋值问题，未保留可靠的 Docker 数值退出码；该限制保留。实际必要断言已完成，日志没有后续脚本异常，不为补退出码再跑验收。测试对象存储在临时容器内完成真实回读，容器退出后清理，不冒充长期发布的业务快照。市场输入是明确标记的合成接线 fixture，不能据此宣称真实候选或市场覆盖已通过。

# 后续工程顺序

1. **Git 集成**：下一次集成动作可将 main 从 ee8c895 fast-forward 到 4ec977c；本次原工作包明确不合 main、不部署，因此此次核验保持该边界。使用 NUC 干净上下文，不在 Mac 既有脏工作树操作；没有新变更或具体失败时，不重复全量测试或独立审查。
2. **下一工程包建议：SEC 历史采集与显式 v3 运行入口**。复用现有采集、归档、限流与重试设施，按 cutoff 和所需申报期间获取必要的 `filings.files` 历史页，保存原始 body，再传给已实现的 v3 parser。让操作入口可以明确选择 v3 并回读输入快照。先在既有小样本验证，不直接扩至全市场；这是建议范围，尚未派发或开始。
3. **单独裁决股本和估值字段语义**：时点普通股本、EPS 加权平均分母、估值稀释桥分别表达。定义确定前，不改变现有投资口径，也不发布依赖含混市值的新正式排名。债务、IFRS/TWD/ADR 映射和金融行业支持继续列为实际缺口。
4. **随后才做完整筛选链小样本验收和规模复测**：真实市场输入、财务输入、规则/评分、报告闭合后，再评估覆盖率、缺失分布与数据采购。L1.9 原容量数字不能当作完整财务链吞吐量。

当前没有足够理由重新审计 L1.3—L1.9 全部成果。新审查随新的实质工程里程碑安排；若发现具体旧缺陷，再追踪其影响范围。

本项目继续遵循《内部项目最小防护与工程纪律 v1.0》：“不得新增该文件第4节所列防护；除第5节停止条件外，不得因范围、沙箱、审计轮次、测试环境或文件清单请求人工批准。”Co-op 可按需顺序派发角色，NUC 运维使用已上线的 OpenClaw 通道。没有数据采购、L2/L3、production 或交易动作，paper-only 边界保持。
