# 跨设备同步说明

目标：把这个研究项目作为 Obsidian 文档库同步，在多台设备上同时保留研究笔记、配置、脚本、数据快照和静态 dashboard。

## 推荐方式

1. 在主设备上打开 Obsidian。
2. 选择“打开已有文件夹作为文档库”。
3. 选择本项目根目录：`美股泡沫研究`。
4. 启用 Obsidian Sync，或把这个文件夹放进你已有的 Obsidian 同步目录。
5. 在其他设备上同步完成后，同样把这个文件夹作为 Obsidian 文档库打开。

## 建议同步的内容

这些内容建议保留同步：

- `*.md`: 研究说明和模型文档。
- `config/`: 手动指标配置。
- `scripts/`: 数据更新和回测脚本。
- `docs/`: 静态 dashboard 页面和展示数据。
- `data/`: 数据缓存、FINRA 文件和回测结果。
- `.github/`: GitHub Actions 自动更新配置。

这些内容不需要手动同步：

- `__pycache__/`
- `*.pyc`
- `.tools/`

## 新设备首次运行

在项目目录打开终端：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
```

更新数据：

```powershell
python scripts/update_data.py
```

启动本地 dashboard：

```powershell
python -m http.server 8000 -d docs
```

浏览器打开：

```text
http://localhost:8000
```

## 日常更新流程

1. 在 Obsidian 里修改研究笔记或手动指标。
2. 如果修改了 `config/indicators.json`，同步后在任意设备运行：

```powershell
python scripts/update_data.py
```

3. 如果修改了底部框架逻辑，运行：

```powershell
python scripts/backtest_bottom_framework.py
python scripts/update_data.py
```

## 注意事项

- `data/` 里有自动下载的数据缓存。同步它可以让新设备离线查看已有结果；如果不同步，也可以通过脚本重新下载。
- `docs/data/dashboard.json` 和 `docs/data/dashboard-inline.js` 是 dashboard 当前快照，建议同步。
- 多设备同时编辑 `config/indicators.json` 容易产生冲突；建议一次只在一台设备上改手动指标。
- 这个项目仍然可以继续用 Git 管理，Obsidian 主要负责文档库式跨设备同步。

